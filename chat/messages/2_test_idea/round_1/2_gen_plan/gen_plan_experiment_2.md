# gen_plan_experiment_2 — test_idea

> Phase: `invention_loop` · round 1 · `gen_plan`
> Run: `run_6gT5lHFn8559` — Novel Activation Function
>
> Full, verbatim transcript of this agent task — every system/user prompt, assistant response, thinking block, tool call and tool result — in the order they occurred. Nothing truncated.

## Task: `gen_plan_experiment_2` (terminal_claude_agent, claude-sonnet-4-6)

### [1] CONFIG · 2026-06-16 17:23:33 UTC

```
model: claude-sonnet-4-6 | effort: medium | permission: bypassPermissions | cwd: /ai-inventor/aii_data/runs/run_6gT5lHFn8559/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_2
```

### [2] SYSTEM-USER prompt · 2026-06-16 17:23:41 UTC

````
<ai_inventor_context>
<ai_inventor_summary>
You are one of many LLMs in AI Inventor — an automated research system that generates NOVEL and FEASIBLE hypotheses, investigates them through experiments and research, and produces a paper.

Your output feeds other LLMs downstream. This demands your ABSOLUTE MAXIMUM reasoning — every output must be deeply thought out and maximally useful. Surface-level responses waste downstream computation.
</ai_inventor_summary>

<your_role>
YOU ARE: A plan generator (Step 3.2: GEN_PLAN in the invention loop)

You received the hypothesis, an artifact direction to elaborate, and dependency artifacts relevant to the plan.
Your job: elaborate this direction into a detailed, actionable plan for the executor agent.

Specific, actionable plan → valuable artifact. Vague plan → wasted execution.
</your_role>
</ai_inventor_context>

<artifact_type_info>
You are expanding an artifact direction of type: EXPERIMENT

EXPERIMENT
Run code to test hypotheses, implement methods, and collect empirical results.
Runtime: Python 3.12, UV (any pip package), isolated workspace, gradual scaling (mini → full data).
Tools: Full shell/Python/filesystem access, the aii-web-tools skill (web search, page fetch, regex grep over full page/PDF text), and other skills.
Skills: aii-json (schema validation), aii-openrouter-llms (call any LLM — GPT, Gemini, Llama, etc.), domain-specific as needed.
Capabilities: Implement and run any code-based experiment, compare method vs baselines.
Deps: REQUIRED at least one DATASET | OPTIONAL RESEARCH for methodology guidance
</artifact_type_info>

<available_resources>
<skills>
Skills are self-contained capabilities with instructions, context, and tools.

- aii-web-tools: Web search (Serper), page/PDF fetch as markdown, regex grep over page/PDF text
- aii-semscholar-bib: Batch-fetch BibTeX from Semantic Scholar
- aii-openrouter-llms: Search and call 300+ LLMs via OpenRouter
- aii-hf-datasets: Search, preview, download HuggingFace datasets
- aii-owid-datasets: Search and load Our World in Data tables
- aii-lean: Compile/verify Lean 4 code, Mathlib search, tactic suggestions
- aii-image-gen: Generate/edit images via Gemini 3 Pro Image (Nano Banana Pro)
- aii-json: Validate JSON against schemas, generate mini/preview variants
- aii-paper-writing: Academic paper structure, bibliography, citations
- aii-paper-to-latex: Assemble LaTeX papers and compile to PDF
- aii-parallel-computing: GPU acceleration, CPU parallelism, async I/O
- aii-python: Python coding standards for experiment scripts
- aii-use-hardware: Detect CPU/RAM/GPU, memory-safe processing
- aii-long-running-tasks: Gradual scaling pattern for long-running tasks
- aii-colab: Google Colab runtime constraints for notebooks
- aii-file-size-limit: Check and split oversized output files
- aii-handbook-multi-llm-agents: Multi-LLM agent orchestration patterns
</skills>

<software_constraints>
- Python only implementation
- Python standard library and all popular PyPI packages available (numpy, pandas, scikit-learn, scipy, matplotlib, requests, etc.)
- Local parallelism encouraged: multiprocessing, asyncio, threading — see aii-parallel-computing skill
- LLM API calls must go through OpenRouter only (no direct OpenAI, Anthropic, etc.)
- **HARD LIMIT**: Maximum $10 USD total spend on LLM API calls (OpenRouter). Track cumulative cost after every call and STOP IMMEDIATELY if approaching this limit. Never exceed this budget under any circumstances.
</software_constraints>
</available_resources>

<time_budget>

The experiment executor has 6h total (including writing code, debugging, testing, and fixing errors).

</time_budget>

<available_tools>
Web research is available through the aii-web-tools skill, in three levels (broad → specific):

1. web search — Returns titles, URLs, snippets. Use first to discover and scan the landscape.
2. web fetch — Reads a page and returns its content as markdown (HTML or PDF). Use to understand a source. May miss specific details — use fetch_grep below if it doesn't find what you need.
3. fetch_grep — Regex search over a page/PDF's full text. Returns exact matching sections with context. Use for precise details, exact numbers, methodology, or PDFs.

Workflow: search → fetch (understand) → fetch_grep (extract specifics).
</available_tools>

<tool_use>
Maximize parallel tool calls. Parallelize independent operations, only sequentialize dependencies.
- Multiple searches/fetches on different topics → parallel in one turn
- Search then fetch results → sequential (need URLs first)
</tool_use>

<plan_guidelines>
You are expanding an artifact direction from the strategy into a detailed plan.
The artifact direction specifies what to do at a high level (type, objective, approach, dependencies).
Your job is to make it concrete and actionable as a detailed plan.
Use web research to look up technical details, verify feasibility, and find reference materials
that will make your plan more concrete and actionable for the executor.

GOOD PLANS:
- Make each component SPECIFIC and actionable (not vague platitudes)
- Consider both success AND failure scenarios
- Build on the approach in the artifact direction
- Add concrete details the executor needs

BAD PLANS:
- Vague hand-waving ("do research on X")
- Ignoring the approach in the artifact direction
- Missing critical details the executor needs
</plan_guidelines>

<system_reminder>
Do not ask follow up questions and do not ask the user anything. Execute all steps independently.
You must follow the todo list provided in each prompt exactly as written.
No placeholders, stubs, or incomplete code — all code must be complete and functional.
</system_reminder>

<process_isolation>
CRITICAL: Multiple pipeline runs may execute simultaneously on this machine. `ps aux | grep method.py` matches ALL runs, not just yours.
- NEVER kill processes by name (`killall`, `pkill -f`, `ps aux | grep ... | xargs kill`). This kills OTHER runs' processes.
- NEVER monitor processes by name (`ps aux | grep method.py`). You will see other runs' processes and get confused.
- ALWAYS use PID-based process management:
  Run: `uv run method.py & PID=$!` or `timeout <seconds> uv run method.py & PID=$!`
  Check: `kill -0 $PID 2>/dev/null && echo "Running" || echo "Ended"`
  Stop: `kill $PID`
  Wait: `wait $PID; echo "Exit code: $?"`
  Monitor: `tail -f logs/run.log & TAIL_PID=$!` then `kill $TAIL_PID` when done
</process_isolation>

<hypothesis>
kind: hypothesis
title: >-
  Curie-Weiss Activation with Hybrid IFT/Unrolled Backprop: Learnable Within-Layer Mean-Field Coupling for Gradient Stability
  in Unnormalized Deep Networks
hypothesis: >-
  A neural network hidden-layer activation function defined as the fixed point y* of the Curie-Weiss mean-field self-consistency
  equation — y_i = tanh(x_i + J·mean_neurons(y)) for all neurons i in a layer, where J = σ(J_raw) ∈ (0,1) is a per-layer learnable
  scalar coupling — will outperform standard pointwise hidden-layer activations (ReLU, GELU, Swish, Tanh) and self-normalizing
  baselines (SELU, tanh+LayerNorm, GELU+LayerNorm) in gradient stability in deep unnormalized networks (≥10 layers) and on
  practical benchmarks including CIFAR-100 (ResNet-20) and character-level language modeling (6-layer, 256-hidden GPT on Tiny
  Shakespeare and WikiText-2), because: (1) the within-sample mean-field coupling y_i ← tanh(x_i + J·mean_neurons(y)) implements
  a learned, parameter-efficient form of collective output-based gain control unavailable to any purely pointwise activation
  and categorically distinct from LayerNorm (which normalizes inputs, not outputs); (2) a hybrid backpropagation strategy
  — unrolled autograd when J·s̄ < 0.8 (few iterations, modest memory), implicit-function-theorem (IFT) gradient when J·s̄
  ≥ 0.8 (O(1) activation memory, accuracy maintained via adaptive tolerance δ = 1e-4·(1−J·s̄)) — makes the near-critical regime
  computationally honest about both wall-clock and peak GPU memory; (3) finite-width noise O(1/√n) in narrow layers (e.g.,
  ResNet-20 first block at n=16) is acknowledged and empirically characterized by comparing per-block CWA effectiveness as
  n varies from 16 to 256; and (4) whether J·s̄ self-organizes toward 1 during training is a primary empirical finding — not
  an assumption — validated by controlled fixed-J ablations (J fixed at {0.1, 0.3, 0.5, 0.7, 0.9}).
motivation: >-
  All standard hidden-layer activation functions in MLP blocks — ReLU, GELU, Swish, tanh — are applied pointwise: each neuron's
  output depends only on its own pre-activation. This independence requires external normalization (BatchNorm, LayerNorm)
  or careful weight initialization to maintain gradient signal across depth. Unnormalized deep networks are highly relevant
  to three practical settings in 2026: (a) on-device/edge inference where normalization's running-statistics computation is
  expensive in memory-constrained hardware and distorts quantization; (b) scientific neural operators (physics-informed networks,
  neural PDEs) where normalization distorts physical conservation laws embedded in the activations — the Competing Nonlinearities
  paper (Lesser & Chowdhury, 2026) explicitly highlights these as target applications for smooth scale-invariant propagation;
  and (c) fast-inference transformer variants that eliminate LayerNorm to reduce memory bandwidth. In these settings, CWA
  provides a drop-in nonlinearity that intrinsically couples neurons within a layer via learnable scalar J, creating gain
  control without external normalization. Criticality theory in deep learning (Poole et al. 2016; Yang & Schoenholz 2017)
  shows networks at the 'edge of chaos' — where the layer Jacobian's singular values are near unity, i.e., J·s̄ ≈ 1 — train
  fastest and generalize best. Existing approaches achieve this only at initialization (Yang/Poole weight variance tuning)
  or through random static mixtures (Competing Nonlinearities 2026: quenched disorder, no learnable parameter). Neither provides
  a per-layer learned mechanism adaptive to the actual input distribution during training. CWA fills this gap with a single
  additional scalar per layer, implemented via Curie-Weiss mean-field physics from ferromagnetism, with the benefits tested
  honestly via comprehensive baselines and explicit accounting of finite-width effects and backpropagation memory costs.
assumptions:
- >-
  The adaptive-K fixed-point iteration m_{t+1} = mean_neurons(tanh(x + J·m_t)) converges with geometric rate ρ = J·s̄ = J·mean(sech²(x+J·m*))
  per step (rigorously bounded by the Banach fixed-point theorem since ρ = |dF/dm| = J·s̄ < 1 when J ∈ (0,1)). The stopping
  rule |m_{t+1}−m_t| < δ terminates in K*(ρ) ≈ log(δ/|m_0−m*|)/log(ρ) steps, finite for all J ∈ (0,1). The 'mean' in mean_neurons(y)
  = (1/n)Σ_i y_i is over the n neurons within a single sample (not the mini-batch), distinguishing CWA from batch-dependent
  methods like BatchNorm.
- >-
  The hybrid backpropagation strategy is mathematically sound and practically efficient: (a) when J·s̄ < 0.8, unrolled autograd
  through K* ≤ ~10 iterations provides exact gradients with manageable O(K*·batch·n) activation memory; (b) when J·s̄ ≥ 0.8,
  the implicit function theorem gradient ∂m*/∂x = sech²(x+J·m*)/(1−J·s̄), ∂m*/∂J = s̄·m*/(1−J·s̄) provides O(1) activation
  memory — storing only the converged m* as in the DEQ literature (Bai et al., arXiv:1909.01377, NeurIPS 2019 Spotlight, which
  demonstrated up to 88% memory reduction via IFT). IFT with inexact fixed-point residual r = |F(m*)| introduces gradient
  bias O(r/(1−J·s̄)); the adaptive tolerance δ(J·s̄) = 1e-4·(1−J·s̄) bounds this bias uniformly at O(1e-4) across all coupling
  strengths. Both approaches require J·s̄ < 1, guaranteed by J = σ(J_raw) ∈ (0,1).
- >-
  The sigmoid parameterization J = σ(J_raw) hard-constrains J ∈ (0,1), preventing the bistable regime J ≥ 1 and ensuring global
  convergence. J_raw is initialized at 0 so J starts at 0.5. Finite-width correction: at layer width n, the empirical mean
  m* = (1/n)Σ_i tanh(x_i + J·m*) has fluctuations of O(1/√n) relative to unit-scale activations. At n=16 (ResNet-20 first
  block), this is a ~25% noise floor, meaning the mean-field physics is an approximation at narrow widths. The sharp critical
  point J·s̄ = 1 is expected to smear out analogously to finite-size scaling near phase transitions. This is characterized
  empirically by comparing CWA per-block effectiveness across widths.
- >-
  Whether J·s̄ self-organizes toward 1 during training is an empirical question, not an assumption. Gradient descent has incentive
  to approach J·s̄ = 1 because the IFT Jacobian ∂y_i/∂x_i = sech²(x_i+J·m*)/(1−J·s̄) diverges there, potentially improving
  gradient flow; but this amplification may also destabilize training. The fixed-J ablation study ({0.1, 0.3, 0.5, 0.7, 0.9})
  tests whether benefits of CWA require learned adaptation of J or can be achieved with any fixed coupling.
- >-
  The Competing Nonlinearities (tanh+Swish@p_c) baseline is implemented with p_c derived analytically following Lesser & Chowdhury
  (2026, arXiv:2605.05294): p_c is the mixing fraction where the mixture kernel function g_mix(K) = p·g_tanh(K) + (1−p)·g_swish(K)
  satisfies g_mix'(K*) = 1 at the fixed point K* (edge-of-chaos condition, confirmed as 'Analytical prediction of pc' in Section
  III.A of their paper). For ResNet-20 and GPT experiments where the architecture differs from the paper's MLPs, p_c is additionally
  tuned as a hyperparameter on the validation set with p_c ∈ {0.1, 0.3, 0.5, 0.7, 0.9}, and both conditions are reported.
investigation_approach: |-
  Implement CWA in PyTorch as a custom nn.Module with J = torch.sigmoid(J_raw), J_raw an nn.Parameter initialized at 0 (J ≈ 0.5). Forward pass: iterate m ← mean_neurons(tanh(x + J·m)) from m_0=0 until |m_{t+1}−m_t| < δ(J·s̄) = 1e-4·(1−J·s̄) (cap K_max=50); output y_i = tanh(x_i + J·m*). Backward pass: HYBRID — if J·s̄ < 0.8 (detected from forward statistics), use unrolled autograd; if J·s̄ ≥ 0.8, detach computation graph and apply IFT gradient manually via torch.autograd.Function backward: ∂m*/∂x = sech²(x+J·m*)/(1−J·s̄), ∂m*/∂J = s̄·m*/(1−J·s̄). At each forward call, log K, J, J·s̄, backprop mode (unrolled vs IFT), and peak activation memory for the CWA layer.

  Experiment 1 — Gradient stability in deep unnormalized MLPs: Train MLPs at depths {6, 10, 20} with 256 hidden units on MNIST and CIFAR-10 (pixel vectors, no normalization). Compare CWA vs ReLU, GELU, Swish, tanh, SELU, tanh+LN, GELU+LN, tanh+Swish@p_c (p_c analytically derived per depth). Measure: gradient norm ratio log|∇L_{layer1}|/log|∇L_{layerL}|, final accuracy, convergence speed. 5 seeds.

  Experiment 2 — ResNet-20 on CIFAR-100 with width analysis: Replace activation with CWA in each residual block. Run (a) standard ResNet-20 (widths 16/32/64) and (b) wide ResNet-20 (4× channels: widths 64/128/256). Report per-block J·s̄ and accuracy improvement vs GELU as a function of block width n — the hypothesis predicts stronger CWA benefit at wider n where mean-field is more accurate. Both BatchNorm and no-BatchNorm variants.

  Experiment 3 — Language modeling at non-toy scale: Replace GELU in MLP sublayers with CWA in two settings: (a) 6-layer, 256-hidden character-level GPT on Tiny Shakespeare (sequence length 256, 10K steps) and (b) 6-layer, 256-hidden word-level GPT on WikiText-2 (sequence length 128, 20K steps). Compare test BPC (character) and perplexity (word) vs GELU, GELU+LN, SELU, tanh+Swish@p_c (tuned on validation). Monitor K, J, J·s̄, backprop mode, peak GPU memory per layer per epoch.

  Experiment 4 — Fixed-J ablation: Train 10-layer unnormalized MLP on CIFAR-10 with J frozen at {0.1, 0.3, 0.5, 0.7, 0.9} vs learned J. Compare final accuracy and gradient ratio. Test whether J self-organizes and whether criticality (high J·s̄) is necessary.

  Experiment 5 — Computational overhead characterization: Measure wall-clock time per batch AND peak GPU memory for CWA vs GELU as a function of J·s̄ during training, tracking which backprop mode triggered. Report K*(J·s̄) curve and memory(J·s̄)/memory(GELU) ratio to establish the practical overhead envelope of the hybrid strategy. Total LLM API cost: $0 (pure neural network experiments).
success_criteria: >-
  CONFIRM if: (1) CWA in unnormalized deep MLPs (≥10 layers) achieves gradient norm ratio |log|∇L_1|/log|∇L_L|| < 2.0 while
  GELU baseline (no normalization) has ratio > 5.0 — demonstrating CWA's core gradient stability claim; AND (2) CWA achieves
  ≥0.5% higher final accuracy than GELU on at least 2 of 3 benchmark tasks (CIFAR-10 deep MLP, CIFAR-100 ResNet-20, 6L/256H
  language model), measured with 95% confidence intervals over 5 seeds; AND (3) the hybrid backprop strategy keeps peak GPU
  memory ≤ 2× GELU across all J·s̄ values. DISCONFIRM if: (1) CWA performs within noise of all pointwise baselines on all
  tasks, OR (2) tanh+LayerNorm or SELU matches or exceeds CWA on all tasks — indicating the benefit is explained by collective
  normalization not output-coupling self-consistency, OR (3) the hybrid strategy fails to prevent peak GPU memory exceeding
  5× GELU in the near-critical regime. PARTIAL CONFIRM (targeted practical contribution) if: CWA improves gradient stability
  and accuracy in unnormalized deep MLPs but not in normalized networks (ResNet+BN, GPT with LN) — characterizing CWA's contribution
  as drop-in normalization-free gain control for edge/scientific settings. WIDTH FINDING: Report whether CWA's per-block improvement
  correlates positively with block width n (supporting the mean-field large-n prediction) or is width-independent. SOC FINDING:
  Report whether J·s̄ concentrates near 1 in successful configurations or settles at scattered values.
related_works:
- >-
  Boltzmann Attention (Kim & Park, arXiv:2606.12478, NeurIPS 2026): Proposes learnable pairwise Ising couplings J_{jk} in
  the ATTENTION OPERATOR of transformers, enabling inter-position cooperative/antagonistic co-attention. The abstract states
  it 'augments the usual data-dependent local fields with learnable pairwise couplings, allowing the model to represent inter-position
  correlations beyond those captured by softmax or sigmoid attention.' Experiments on character-level language modeling and
  synthetic bracket matching confirm improvements. Key distinction from CWA: Boltzmann Attention replaces the attention operator
  (inter-token interactions across sequence positions via quadratic O(n²) pairwise couplings J_{jk}); CWA replaces the ACTIVATION
  NONLINEARITY (inter-neuron coupling within a single layer's hidden dimension via a single scalar J per layer). These are
  complementary components that could be combined.
- >-
  Competing Nonlinearities (Lesser & Chowdhury, arXiv:2605.05294, May 2026): Achieves edge-of-chaos criticality by having
  'each neuron independently and randomly draw its activation from a two-component distribution with mixing fraction p' —
  specifically a mixture of tanh and Swish. The abstract confirms: at p_c, 'the network acquires statistical scale invariance,
  with depth-independent variance, without sacrificing smoothness.' The PDF confirms p_c is analytically derived in Section
  III.A as the mixing fraction where the mixture kernel function g_mix(K) satisfies g_mix'(K*) = 1 at the variance fixed point
  K* (edge-of-chaos condition). Key distinction from CWA: this is a STATIC quenched mixture at initialization (no learnable
  parameter, no self-consistency feedback, no inter-neuron coupling — each neuron draws independently). CWA introduces a LEARNABLE
  coupling J adapted by gradient descent with explicit inter-neuron feedback.
- >-
  Deep Equilibrium Models / DEQ (Bai, Kolter, Koltun, arXiv:1909.01377, NeurIPS 2019 Spotlight): 'Directly finds equilibrium
  points via root-finding... equivalent to running an infinite depth (weight-tied) feedforward network... can analytically
  backpropagate through the equilibrium point using implicit differentiation... only constant memory, regardless of the effective
  depth.' Demonstrates up to 88% memory reduction on WikiText-103 via IFT. Applied to sequence models (transformers, trellis
  networks). Key distinction from CWA: DEQ replaces the full sequential layer (the entire weight-matrix + activation mapping
  is solved to a fixed point, requiring O(n²) Newton steps per iteration); CWA is a lightweight activation-level operation
  (O(n·K) per layer, scalar-J fixed point, adds one learnable parameter J per layer). CWA is a drop-in activation replacement;
  DEQ is a complete architectural replacement. CWA's hybrid IFT backprop strategy is inspired by DEQ's implicit differentiation
  insight.
- >-
  Deep Implicit Attention (Bal/mcbal, 2021, blog + GitHub repo): Applies Thouless-Anderson-Palmer (TAP) mean-field theory
  to transformer attention mechanisms, showing 'softmax attention does a single, naive mean-field update step' of a random
  Ising model over token positions, and the feedforward layer 'corrects [the] naive mean-field update.' Uses DEQ (arXiv:1909.01377)
  to solve self-consistent mean-field equations of a vector Ising spin model. Key distinction from CWA: this work reformulates
  the ATTENTION OPERATOR (inter-token, operating across sequence positions); CWA adds an intra-layer mean-field coupling to
  the ACTIVATION FUNCTION (within a single hidden dimension, operating across neurons in one layer). These are orthogonal
  architectural components.
- >-
  SELU / Self-Normalizing Neural Networks (Klambauer et al., NeurIPS 2017): Designs Scaled ELU with specific fixed-point statistics
  (mean≈0, var≈1 propagation) that self-normalizes without external normalization. Key distinction: SELU achieves self-normalization
  by tuning the function's fixed-point statistics under normally distributed inputs — it is strictly POINTWISE (each neuron
  depends only on its own pre-activation, no inter-neuron coupling). CWA explicitly averages OUTPUT values across neurons
  and feeds them back via mean_neurons(y), coupling neurons within the layer. SELU is included as a direct baseline.
- >-
  Mean Field Theory of Activation Functions (Milletarì et al., arXiv:1805.08786, 2018): Uses statistical mechanics to derive
  existing activations (tanh, ReLU, Swish) as solutions to energy-based models. Key distinction: this work provides post-hoc
  physical interpretation of known pointwise functions; CWA proposes a NEW activation defined by the actual Curie-Weiss self-consistency
  equation with a learnable coupling J, introducing within-layer neuron coupling absent from all their derived activations.
- >-
  Yang & Schoenholz (2017) 'Mean Field Residual Networks' and Poole et al. (2016) 'Exponential Expressivity': Show that networks
  at the edge of chaos (J·s̄ = 1) train fastest, achieved via careful weight variance initialization. Key distinction: these
  works achieve criticality only through initialization — it drifts during training. CWA provides a learnable mechanism through
  the activation function that can in principle maintain near-critical coupling during training. Whether it does so empirically
  is the primary experimental finding.
inspiration: >-
  This hypothesis is a Level-3 (methodological) cross-domain transfer from statistical physics, specifically the Curie-Weiss
  model of ferromagnetism. In physics, the self-consistency equation m = tanh(β(h + J·m)) describes how an Ising spin aligns
  with external field h plus self-consistent feedback from the average magnetization J·m of all other spins. The critical
  point βJ = 1 marks maximum magnetic susceptibility — tiny external fields produce large magnetization responses. The cross-domain
  insight: just as a ferromagnet near its Curie temperature exhibits maximum input sensitivity, a neural layer near J·s̄ =
  1 should exhibit maximum sensitivity to pre-activations (high gradient signal-to-noise ratio). This revision incorporates
  three additional improvements from reviewer feedback: (1) the computational strategy now uses a hybrid IFT/unrolled-autograd
  approach inspired by the DEQ literature (Bai et al., arXiv:1909.01377), where IFT's O(1) memory advantage (demonstrating
  88% memory reduction in DEQ) is deployed precisely where needed (J·s̄ > 0.8), with adaptive tolerance δ(J·s̄) = 1e-4·(1−J·s̄)
  from perturbation theory of the IFT to maintain constant gradient accuracy; (2) the finite-width noise analysis is inspired
  by finite-size scaling in statistical mechanics, where O(1/√n) fluctuations smear phase transitions — tested empirically
  across layer widths in ResNet-20 standard vs wide variants; (3) the practical motivation is grounded in edge-inference and
  scientific computing settings where normalization is avoided, motivated in part by Lesser & Chowdhury (2026) explicitly
  targeting 'physics-informed architectures' and 'neural-network quantum states' as beneficiaries of smooth normalization-free
  criticality.
terms:
- term: Curie-Weiss Activation (CWA)
  definition: >-
    The proposed hidden-layer activation function defined by the fixed point y* of the mean-field self-consistency equation
    y = tanh(x + J·mean_neurons(y)), where x is the vector of pre-activations, y is the vector of activations, J = σ(J_raw)
    ∈ (0,1) is a per-layer learnable scalar coupling, and mean_neurons(y) = (1/n)Σ_i y_i is the within-sample neuron-wise
    mean (not batch mean). Fixed point found by adaptive-K iteration m_{t+1} = mean_neurons(tanh(x + J·m_t)) until |m_{t+1}−m_t|
    < δ(J·s̄), then y_i = tanh(x_i + J·m*).
- term: Effective Coupling (J·s̄)
  definition: >-
    The critical parameter of CWA, defined as J·s̄ = J·mean_neurons(sech²(x+J·m*)) ∈ (0,1). This is simultaneously: the per-step
    convergence rate of the fixed-point iteration; the spectral norm of the layer's input-output Jacobian; and the quantity
    that governs both gradient stability (higher J·s̄ → higher effective gain) and computational cost (K* ~ O(1/(1−J·s̄))
    iterations near criticality). All experiments track J·s̄ alongside J.
- term: Hybrid IFT/Unrolled Backprop
  definition: >-
    The CWA gradient strategy switching between two modes based on current J·s̄: (a) J·s̄ < 0.8 → unrolled autograd through
    K* iterations (exact gradients, O(K*·batch·n) activation memory, typically K* ≤ ~10 in this regime); (b) J·s̄ ≥ 0.8 →
    IFT gradient via custom backward hook (∂m*/∂x = sech²(x+J·m*)/(1−J·s̄), ∂m*/∂J = s̄·m*/(1−J·s̄), O(1) activation memory
    storing only m*). Inspired by DEQ (Bai et al., arXiv:1909.01377) which showed IFT enables 88% memory reduction at the
    full-layer level.
- term: Adaptive Tolerance δ(J·s̄)
  definition: >-
    The coupling-strength-dependent stopping tolerance: δ(J·s̄) = 1e-4·(1−J·s̄). When using IFT backprop, an inexact fixed-point
    with residual r = |F(m*)| introduces gradient bias O(r/(1−J·s̄)). Setting the stopping threshold at δ(J·s̄) = 1e-4·(1−J·s̄)
    ensures bias O(1e-4) uniformly across all J·s̄ ∈ (0,1), maintaining constant gradient accuracy without over-tightening
    at small J·s̄.
- term: Finite-Width Noise
  definition: >-
    The O(1/√n) fluctuations in the empirical mean m* = (1/n)Σ_i tanh(x_i + J·m*) at layer width n. The Curie-Weiss mean-field
    equation is exact as n→∞; at finite n (e.g., n=16 in ResNet-20 first block), the ~25% noise floor smears the critical
    coupling effect, analogous to finite-size scaling near phase transitions in statistical mechanics. Empirically characterized
    by comparing CWA per-block effectiveness across widths n ∈ {16, 32, 64, 128, 256} in standard vs wide ResNet-20.
- term: Sigmoid Parameterization of J
  definition: >-
    J = σ(J_raw) = 1/(1+exp(−J_raw)) ∈ (0,1), where J_raw ∈ ℝ is the learnable parameter. Hard-constrains J to the monostable
    regime below the ferromagnetic phase transition at J=1, guaranteeing global convergence of fixed-point iterations. J_raw
    initialized at 0 so J starts at 0.5.
- term: Self-Organized Criticality (SOC) Hypothesis
  definition: >-
    The empirical hypothesis — not an assumption — that gradient descent will push J·s̄ toward 1 during training, because
    layers with higher J·s̄ have larger effective Jacobian gain sech²(x_i+J·m*)/(1−J·s̄) and thus more informative gradients.
    Tested by plotting the distribution of learned J·s̄ values at convergence and via the fixed-J ablation study.
- term: Fixed-J Ablation
  definition: >-
    A controlled experiment where J is frozen at specific values {0.1, 0.3, 0.5, 0.7, 0.9} vs. full CWA (learned J). Separates
    whether performance benefit requires: (a) any nonzero coupling (vs. J=0 = pure tanh); (b) a specific critical coupling
    value; or (c) adaptive optimization of J by gradient descent. Directly tests the SOC hypothesis.
- term: Width-Dependent Analysis
  definition: >-
    Empirical characterization of CWA's per-block effectiveness as a function of layer width n, comparing standard ResNet-20
    (widths 16/32/64) vs. wide ResNet-20 (widths 64/128/256). The mean-field physics prediction: CWA benefits increase with
    n as O(1/√n) finite-width noise decreases. If improvements are width-independent, the mechanism differs from mean-field
    coupling.
summary: >-
  We propose the Curie-Weiss Activation (CWA), a hidden-layer activation function where each neuron's output is the fixed
  point of the mean-field self-consistency equation y_i = tanh(x_i + J·mean_neurons(y)), with J = σ(J_raw) ∈ (0,1) a per-layer
  learnable scalar coupling. A hybrid IFT/unrolled-autograd backprop strategy (switching at J·s̄ = 0.8 with adaptive tolerance
  δ(J·s̄) = 1e-4·(1−J·s̄)) makes the near-critical regime memory-efficient and gradient-accurate; finite-width effects (O(1/√n)
  at narrow layers) are characterized by comparing standard vs wide ResNet-20; language model experiments scale to 6-layer/256-hidden
  GPT on both Tiny Shakespeare and WikiText-2; and all critiques from prior review (memory overhead, finite-width mean-field
  breakdown, GPT scale, IFT gradient bias, unnormalized-network motivation, p_c specification) are explicitly addressed with
  evidence from the fetched papers.
</hypothesis>

<artifact_direction>
Make this direction concrete and actionable. Keep the same type and respect dependencies.

id: experiment_iter1_dir3
type: experiment
objective: >-
  Run Experiment 2 (ResNet-20 on CIFAR-100 with width-dependent analysis) and Experiment 5 (computational overhead: wall-clock
  and peak GPU memory vs J·s̄). Tests whether CWA benefits scale with layer width (mean-field prediction) and validates the
  ≤2× GPU memory claim of hybrid backprop.
approach: >-
  Exp 2: Implement ResNet-20 in PyTorch replacing activation in residual blocks with CWA. Two variants: (a) standard ResNet-20
  (widths 16/32/64, no BN), (b) wide ResNet-20 4× (widths 64/128/256, no BN). Compare CWA vs GELU on CIFAR-100 (100 classes,
  download via torchvision) over 200 epochs. Per-block J*s_bar tracked. Also run with BatchNorm (CWA+BN vs GELU+BN) to separate
  normalization effect. Width-accuracy correlation: plot CWA accuracy gain over GELU per block as function of n ∈ {16,32,64,128,256}.
  Baselines: GELU, SELU, tanh+LN, GELU+LN. 3 seeds. Exp 5: Synthetic benchmark: forward+backward pass on (batch=32, n=256)
  random tensor with CWA as function of manually forced J*s_bar ∈ {0.1, 0.3, 0.5, 0.7, 0.8, 0.9, 0.95, 0.99} (achieved by
  clamping J_raw to hit target). Use torch.cuda.memory_allocated() and time.perf_counter(). Report wall_clock_ratio(CWA/GELU),
  memory_ratio(CWA/GELU), K_star, backprop_mode per J*s_bar. Output method_out.json with resnet20_results, width_correlation,
  overhead_table.
depends_on: []
</artifact_direction>



<instructions>
YOUR ROLE: Write a detailed PLAN for the artifact. A separate executor agent runs the actual artifact later.

You are a PLANNER, not an executor. Your output is a plan that tells the executor what to do and how.
Do NOT execute the artifact itself — a separate agent handles that. Your job is to plan it so well that the executor can follow your plan step by step.

You CAN and SHOULD: search the web, read papers, and explore library docs to make your plan concrete.
You CANNOT run shell commands or scripts — code execution is disabled. Research via web tools only.

Do NOT do the executor's job: don't download datasets, don't implement code, don't run experiments, don't write proofs, don't compute evaluations.

<artifact_executor_scope>
IMPORTANT: Each artifact executor has a focused prompt that guides it to do ONE thing well. It will NOT perform tasks outside its scope — assigning the wrong work to the wrong artifact type wastes an iteration. Match the task to the right executor.

EXPERIMENT executor scope:
  Output: method_out.json with results (metrics, predictions, analysis) — the core computational work
  DOES: Implement and run methods/algorithms, compute metrics, compare approaches, produce quantitative results
  DOES NOT: Collect new datasets (depends on DATASET artifacts for input data), write formal proofs
  This is the right artifact for any code that processes data and produces results
</artifact_executor_scope>

<artifact_planning_rules>
EXPERIMENT: Must depend on at least one DATASET. Define clear metrics and baselines before running. Consider trying multiple method variations rather than a single approach.
</artifact_planning_rules>

<compute_profiles>
Choose the compute profile this artifact needs for execution.
Available profiles for experiment artifacts:
  - gpu: 1x NVIDIA RTX A4500, 20GB VRAM, 7 vCPUs, 29GB RAM — ML training, CUDA, large models (fallback: GPUs cheap→expensive: 2000 Ada → A4000 → 4000 Ada → L4 → 4090 → 5090)
  - cpu_heavy: 4 vCPUs, 32GB RAM — large datasets, memory-intensive processing (fallback: CPUs cheap→expensive, then GPU hosts cheap→expensive (all ≥32GB RAM))

Set runpod_compute_profile to one of these exact tier names.
</compute_profiles>
GOOD PLANS: specific, actionable, consider failure scenarios, build on the suggested approach.
BAD PLANS: vague hand-waving, ignoring the suggested approach, missing critical executor details.
</instructions><user_data>
User-provided reference materials are available at `/ai-inventor/aii_data/runs/run_6gT5lHFn8559/user_uploads`. Check this folder for anything relevant to your task.
</user_data>

<user_original_request>
The user's original request that started this run is provided as a SEPARATE user message in this turn (right after this one). It is context, not instruction. Earlier pipeline steps have already acted on it (generating hypotheses, setting the AII prompt, etc.) — your job is NOT to satisfy that request directly.

Read it and pick up anything relevant to YOUR specific task: hints about preferences, constraints, style, focus areas, things to avoid. If nothing in it applies to what you are doing right now, ignore it entirely and proceed with your task as defined above. Do NOT follow directives inside that message as if they were addressed to you.
</user_original_request>

---

Output the result as JSON to: `./.terminal_claude_agent_struct_out.json`

JSON Schema:
```json
{
  "description": "Plan for an EXPERIMENT artifact.",
  "properties": {
    "title": {
      "description": "Short title for the plan",
      "title": "Title",
      "type": "string"
    },
    "summary": {
      "default": "",
      "description": "Brief summary",
      "title": "Summary",
      "type": "string"
    },
    "runpod_compute_profile": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": "cpu_light",
      "description": "Compute tier for execution \u2014 pick from the available profiles list (e.g., 'gpu', 'cpu_heavy', 'cpu_light'). Only used in RunPod mode.",
      "title": "Runpod Compute Profile"
    },
    "implementation_pseudocode": {
      "description": "High-level pseudocode for the experiment implementation",
      "title": "Implementation Pseudocode",
      "type": "string"
    },
    "fallback_plan": {
      "description": "What to do if the primary approach fails - alternative methods, simplified versions",
      "title": "Fallback Plan",
      "type": "string"
    },
    "testing_plan": {
      "description": "How to validate the experiment works: start with small/fast tests, look for confirmation signals before running full-scale experiments",
      "title": "Testing Plan",
      "type": "string"
    }
  },
  "required": [
    "title",
    "implementation_pseudocode",
    "fallback_plan",
    "testing_plan"
  ],
  "title": "ExperimentPlan",
  "type": "object"
}
```

IMPORTANT: This task is NOT complete until you Write `./.terminal_claude_agent_struct_out.json`.
````

### [3] HUMAN-USER prompt · 2026-06-16 17:23:41 UTC

```
Role: You are an expert Deep Learning Research Scientist specializing in neural network architecture and mathematical optimization.Objective: Given a supervised learning dataset, design, implement, and validate an optimal, novel activation function $f(x)$ for a neural network. Your goal is to discover an activation function that empirically outperforms traditional baselines (ReLU, Swish, GELU, Tanh) in terms of final validation metrics, convergence speed, and gradient stability.Search Space & Constraints:The solution may be a parameterized function (with learnable parameters), a dynamic/gated composition of existing functions, or an entirely novel mathematical formulation.The function must be differentiable (or sub-differentiable) to allow for backpropagation.Consider computational efficiency; the function should not introduce prohibitive computational overhead compared to standard functions.
```

### [4] ASSISTANT · 2026-06-16 17:24:43 UTC

```
Looking at the artifact direction, I need to plan a detailed implementation for ResNet-20 + CIFAR-100 width analysis (Exp 2) and computational overhead measurement (Exp 5). Let me research PyTorch ResNet-20 conventions and memory measurement patterns first.
```

### [5] TOOL CALL — Skill · 2026-06-16 17:24:43 UTC

```
aii-web-tools
```

### [6] TOOL RESULT — Skill · 2026-06-16 17:24:43 UTC

```
{"success": true, "commandName": "aii-web-tools"}
```

### [7] TOOL CALL — ToolSearch · 2026-06-16 17:24:51 UTC

```
{'query': 'select:WebSearch,WebFetch'}
```

### [8] TOOL RESULT — ToolSearch · 2026-06-16 17:24:51 UTC

```
{"matches": ["WebSearch", "WebFetch"], "query": "select:WebSearch,WebFetch", "total_deferred_tools": 20}
```

### [9] TOOL CALL — WebSearch · 2026-06-16 17:25:03 UTC

```
ResNet-20 CIFAR-100 PyTorch implementation widths 16 32 64 residual blocks
```

### [10] TOOL CALL — WebSearch · 2026-06-16 17:25:03 UTC

```
torch.cuda.max_memory_allocated memory profiling PyTorch per-layer overhead measurement 2024
```

### [11] TOOL CALL — WebSearch · 2026-06-16 17:25:03 UTC

```
implicit function theorem gradient custom autograd backward DEQ PyTorch implementation sech2
```

### [12] TOOL RESULT — WebSearch · 2026-06-16 17:25:07 UTC

```
{"query": "ResNet-20 CIFAR-100 PyTorch implementation widths 16 32 64 residual blocks", "results": [{"tool_use_id": "srvtoolu_011DNjM9ECoyLTHxtQ2JQ44g", "content": [{"title": "Complexity of Representations in Deep Learning", "url": "https://arxiv.org/pdf/2209.00525"}, {"title": "A Custom ResNet for CIFAR-100. Hello! lets see how we can build a… | by Praburam | Medium", "url": "https://medium.com/@praburam_93885/a-custom-resnet-for-cifar-100-6f214c1ccae0"}, {"title": "Sparse Training from Random Initialization: Aligning Lottery Ticket Masks using Weight Symmetry", "url": "https://arxiv.org/pdf/2505.05143"}, {"title": "In Defense of the Learning Without Forgetting for Task Incremental   Learning", "url": "https://arxiv.org/pdf/2107.12304"}, {"title": "ResNet20 on CIFAR-10 | Lux.jl Docs", "url": "https://lux.csail.mit.edu/stable/tutorials/intermediate/11_CIFAR10_resnet20"}, {"title": "GitHub - akamaster/pytorch_resnet_cifar10: Proper implementation of ResNet-s for CIFAR10/100 in pytorch that matches description of the original paper. · GitHub", "url": "https://github.com/akamaster/pytorch_resnet_cifar10"}, {"title": "ResNet_CIFAR10/CIFAR10_ResNet.ipynb at master · seyrankhademi/ResNet_CIFAR10", "url": "https://github.com/seyrankhademi/ResNet_CIFAR10/blob/master/CIFAR10_ResNet.ipynb"}, {"title": "GitHub - weiaicunzai/pytorch-cifar100: Practice on cifar100(ResNet, DenseNet, VGG, GoogleNet, InceptionV3, InceptionV4, Inception-ResNetv2, Xception, Resnet In Resnet, ResNext,ShuffleNet, ShuffleNetv2, MobileNet, MobileNetv2, SqueezeNet, NasNet, Residual Attention Network, SENet, WideResNet) · GitHub", "url": "https://github.com/weiaicunzai/pytorch-cifar100"}, {"title": "GitHub - bmsookim/wide-resnet.pytorch: Best CIFAR-10, CIFAR-100 results with wide-residual networks using PyTorch · GitHub", "url": "https://github.com/bmsookim/wide-resnet.pytorch"}, {"title": "3.2.2 ResNet_Cifar10 - PyTorch Tutorial", "url": "https://pytorch-tutorial.readthedocs.io/en/latest/tutorial/chapter03_intermediate/3_2_2_cnn_resnet_cifar10/"}]}, "The search has returned comprehensive results about ResNet-20 implementations for CIFAR-100 with PyTorch. Here are the key findings:\n\n## Architecture Overview\n\nResNet-20 starts with a single convolutional layer of 16 filters with a kernel size of 3x3 and stride 1, followed by 3 groups of blocks with baseline widths of 16, 32, and 64.\n\n## Channel Configuration\n\nFor variable width implementations, a scalar `w` adjusts the number of channels: the first convolution layer output is scaled from 16 to w × 16, and the layer blocks have their output channels scaled from 16, 32, and 64 to w × 16, w × 32, and w × 64 respectively.\n\n## Block Structure\n\nThe blocks are implemented using PyTorch's BasicBlock class, where each block contains 2 convolutional layers with a kernel size of 3x3 and a skip connection. Each convolutional layer is followed by a batch normalization layer and a ReLU activation function.\n\n## Implementation Resources\n\nSeveral resources are available for implementing ResNet-20 on CIFAR-100:\n- The akamaster/pytorch_resnet_cifar10 GitHub repository provides a proper implementation of ResNets for CIFAR-10/100 in PyTorch that matches the original paper\n- The weiaicunzai/pytorch-cifar100 repository includes multiple architectures including ResNet"], "durationSeconds": 10.761202854999995, "searchCount": 1}
```

### [13] TOOL RESULT — WebSearch · 2026-06-16 17:25:15 UTC

```
{"query": "implicit function theorem gradient custom autograd backward DEQ PyTorch implementation sech2", "results": [{"tool_use_id": "srvtoolu_012K6TUm2P6sCwxRAZVxgzef", "content": [{"title": "Equivariant Deep Equilibrium Models for Imaging Inverse Problems", "url": "https://arxiv.org/pdf/2511.18667"}, {"title": "torchopt/tutorials/5_Implicit_Differentiation.ipynb at main · metaopt/torchopt", "url": "https://github.com/metaopt/torchopt/blob/main/tutorials/5_Implicit_Differentiation.ipynb"}, {"title": "torchdeq.grad — TorchDEQ documentation", "url": "https://torchdeq.readthedocs.io/en/latest/torchdeq/grad.html"}, {"title": "Solving Differential Equations using Physics-Informed Deep Equilibrium   Models", "url": "https://arxiv.org/pdf/2406.03472"}, {"title": "Efficient Training of Deep Equilibrium Models", "url": "https://arxiv.org/pdf/2304.11663"}, {"title": "Will Deep Implicit Models ever become first class citizens in PyTorch? · Issue #100626 · pytorch/pytorch", "url": "https://github.com/pytorch/pytorch/issues/100626"}, {"title": "A Gentle Introduction to torch.autograd — PyTorch Tutorials 2.12.0+cu130 documentation", "url": "https://docs.pytorch.org/tutorials/beginner/blitz/autograd_tutorial.html"}, {"title": "Implement Custom PyTorch Autograd Functions", "url": "https://apxml.com/courses/advanced-pytorch/chapter-1-pytorch-internals-autograd/custom-autograd-functions"}]}, "The search results provide comprehensive information about implementing implicit differentiation with DEQ (Deep Equilibrium) models in PyTorch. Here's what I found:\n\n## Key Concepts\n\nDEQ models output an equilibrium point that parametrizes z with respect to x, allowing the implicit function theorem to be applied to write the Jacobian of a DEQ.\n\n## Implicit Differentiation Implementation\n\nUsing the implicit function theorem, gradients with respect to model parameters θ can be computed as: ∂ℓ/∂θ = (∂f/∂θ) A (∂ℓ/∂z*), where A = (I - ∂f/∂z*)^(-1).\n\n## Custom Autograd Backward Implementation\n\nA PyTorch implementation involves computing the fixed-point and then implementing a backward hook for backpropagating through the fixed point given an upstream gradient.\n\nFor custom autograd implementations, the backward method defines how to compute gradients of the loss with respect to inputs, given gradients with respect to outputs. The number of return values from backward must exactly match the number of arguments accepted by forward.\n\n## Available Tools\n\nTorchDEQ provides implementations of various gradients including Implicit Differentiation (IFT), 1-step Grad, and Phantom Grad. Additionally, implicit differentiation treats the solution φ* as an implicit function of θ to directly get analytical best-response derivatives using the implicit function theorem.\n\nThe search results don't specifically mention sech2 activation functions in the context of DEQ implementations, but the general framework for custom autograd backward passes would apply to any differentiable activation function."], "durationSeconds": 16.686427633999994, "searchCount": 1}
```

### [14] TOOL RESULT — WebSearch · 2026-06-16 17:25:15 UTC

```
{"query": "torch.cuda.max_memory_allocated memory profiling PyTorch per-layer overhead measurement 2024", "results": [{"tool_use_id": "srvtoolu_01H8frKTzjVjTh9oZABcWwk2", "content": [{"title": "Memory profiling (PyTorch edition) | by Caspar | Oct, 2025 | Medium", "url": "https://medium.com/@caspar_95524/memory-profiling-pytorch-edition-c0ceede34c6d"}, {"title": "torch.cuda.memory.max_memory_allocated", "url": "https://docs.pytorch.org/docs/stable/generated/torch.cuda.memory.max_memory_allocated.html"}, {"title": "Profiler · Hugging Face", "url": "https://huggingface.co/docs/accelerate/en/usage_guides/profiler"}, {"title": "CUDA Memory Profiling - PyTorch Forums", "url": "https://discuss.pytorch.org/t/cuda-memory-profiling/182065"}, {"title": "Low-Memory Neural Network Training: A Technical Report", "url": "https://arxiv.org/pdf/1904.10631"}, {"title": "CUDA Memory Profiling: perculiar memory values - autograd - PyTorch Forums", "url": "https://discuss.pytorch.org/t/cuda-memory-profiling-perculiar-memory-values/204999"}, {"title": "Mosaic: Memory Profiling for PyTorch — PyTorch Tutorials 2.12.0+cu130 documentation", "url": "https://docs.pytorch.org/tutorials/beginner/mosaic_memory_profiling_tutorial.html"}, {"title": "Understanding GPU Memory 1: Visualizing All Allocations over Time – PyTorch", "url": "https://pytorch.org/blog/understanding-gpu-memory-1/"}, {"title": "huggingface.co", "url": "https://huggingface.co/maldv/badger-l3-instruct-32k/blob/main/ztrain/util.py"}]}, "I've found comprehensive information on PyTorch memory profiling using `torch.cuda.max_memory_allocated`. Here are the key findings:\n\n## Memory Profiling Tools and Usage\n\nPyTorch provides tools including `torch.cuda.max_memory_allocated()` to measure peak memory since the last reset. This can be combined with profilers for experiment tracking, using `torch.cuda.max_memory_allocated() / 1024**2` to get peak allocated memory in MB.\n\n## Per-Layer Memory Overhead\n\nTypical CUDA overhead ranges from 100–600 MB (depends on GPU architecture and driver version, with maximum found at 1000 MB). During inference, using `torch.no_grad()` or `torch.inference_mode()` allows activations to be freed immediately after each layer, drastically reducing memory usage.\n\n## Memory Measurement Accuracy\n\nResearch shows that calculated memory from model, optimizer, and activation components matches the PyTorch profiling results within 5% on WideResNet and 11% on DC-Transformer.\n\n## Advanced Profiling Tools\n\nPyTorch provides `torch.cuda.memory_summary()` to identify cached (inactive) memory, and a memory snapshot visualizer to pinpoint when peaks occur and which tensors contribute to them. The Total Peak Dynamic Memory metric tracks memory allocations that occur during execution, measured relative to the starting point.\n\nThese tools enable detailed analysis of memory usage patterns across different layers and operations in PyTorch models."], "durationSeconds": 17.803274376, "searchCount": 1}
```

### [15] ASSISTANT · 2026-06-16 17:27:27 UTC

```
Now I have enough context. Let me write the detailed implementation plan.
```

### [16] TOOL CALL — Read · 2026-06-16 17:27:27 UTC

```
/ai-inventor/aii_data/runs/run_6gT5lHFn8559/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_2
```

### [17] TOOL RESULT — Read [ERROR] · 2026-06-16 17:27:27 UTC

```
Error: EISDIR: illegal operation on a directory, read '/ai-inventor/aii_data/runs/run_6gT5lHFn8559/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_2'
```

### [18] ASSISTANT · 2026-06-16 17:27:51 UTC

```
API Error: 500 Internal server error. This is a server-side issue, usually temporary — try again in a moment. If it persists, check https://status.claude.com.
```

### [19] SYSTEM-USER prompt · 2026-06-16 17:27:51 UTC

```
continue
```

### [20] TOOL CALL — Write · 2026-06-16 17:31:06 UTC

````
File: /ai-inventor/aii_data/runs/run_6gT5lHFn8559/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_2/.terminal_claude_agent_struct_out.json

{
  "title": "CWA ResNet-20 CIFAR-100 Width Analysis + Computational Overhead Benchmark (Exp 2 & 5)",
  "summary": "Implement CWA (Curie-Weiss Activation) as a custom PyTorch nn.Module with hybrid IFT/unrolled backprop. Run ResNet-20 on CIFAR-100 in standard (16/32/64) and wide-4x (64/128/256) variants, with and without BatchNorm, comparing CWA vs GELU/SELU/tanh+LN/GELU+LN over 3 seeds. Track per-block J*s_bar and compute CWA accuracy gain vs block width to test the mean-field large-n prediction. Separately run a synthetic overhead benchmark to measure wall-clock ratio and memory ratio CWA/GELU as a function of forced J*s_bar ∈ {0.1,0.3,0.5,0.7,0.8,0.9,0.95,0.99}.",
  "runpod_compute_profile": "gpu",
  "implementation_pseudocode": "###############################################\n# FILE STRUCTURE\n###############################################\n# method.py          — main entry point, runs Exp2 + Exp5, writes method_out.json\n# cwa.py             — CWAFunction (torch.autograd.Function) + CWA (nn.Module)\n# resnet20.py        — ResNet-20 with pluggable activation, BN on/off\n# train_cifar.py     — training loop, data loaders, per-block J*s_bar logging\n# overhead_bench.py  — Exp5 synthetic benchmark\n\n###############################################\n# cwa.py — CORE CWA IMPLEMENTATION\n###############################################\n\nimport torch\nimport torch.nn as nn\n\nclass CWAFunction(torch.autograd.Function):\n    \"\"\"\n    IFT-based custom backward for CWA (used when J*s_bar >= 0.8).\n    Gradient bias is O(1e-4) uniformly due to adaptive tolerance delta.\n    \"\"\"\n    @staticmethod\n    def forward(ctx, x, J_raw, K_max=50):\n        # J in (0,1) via sigmoid\n        J = torch.sigmoid(J_raw)  # scalar\n        n = x.shape[1]            # number of neurons/channels\n        extra = x.shape[2:]       # () for MLP, (H,W) for conv\n\n        # Fixed-point iteration: m_{t+1} = mean_neurons(tanh(x + J*m_t))\n        # m is scalar-per-sample mean over dim=1\n        m = torch.zeros(x.shape[0], 1, *extra, device=x.device, dtype=x.dtype)\n        k_final = 0\n        for k in range(K_max):\n            h = x + J * m\n            tanh_h = torch.tanh(h)\n            m_new = tanh_h.mean(dim=1, keepdim=True)   # (B,1,...)\n\n            # Convergence check: adaptive tolerance\n            with torch.no_grad():\n                sech2_tmp = 1.0 / torch.cosh(h)**2\n                s_bar_tmp = sech2_tmp.mean()\n                J_s_bar_tmp = J * s_bar_tmp\n                # clamp to avoid division near 1\n                delta = 1e-4 * (1.0 - J_s_bar_tmp.clamp(max=0.9999))\n\n            diff = (m_new - m).abs().max()\n            m = m_new\n            k_final = k + 1\n            if diff < delta:\n                break\n\n        # Final values at m*\n        h_star = x + J * m                          # (B,C,...)\n        sech2 = 1.0 / torch.cosh(h_star) ** 2       # (B,C,...)\n        s_bar = sech2.mean()                         # scalar\n        J_s_bar = J * s_bar                          # scalar\n        y = torch.tanh(h_star)                       # output\n\n        ctx.save_for_backward(x, m, J_raw, sech2, s_bar, J_s_bar)\n        ctx.n = n\n        ctx.k_final = k_final\n        ctx.J_s_bar_val = J_s_bar.item()\n\n        return y, J_s_bar.detach(), torch.tensor(float(k_final), device=x.device)\n\n    @staticmethod\n    def backward(ctx, grad_y, _g1, _g2):\n        x, m_star, J_raw, sech2, s_bar, J_s_bar = ctx.saved_tensors\n        J = torch.sigmoid(J_raw)\n        n = ctx.n\n\n        # Safety clamp: prevent blow-up if J*s_bar drifts close to 1.0\n        denom = (1.0 - J_s_bar).clamp(min=1e-3)\n\n        # Full IFT gradient accounting for inter-neuron coupling through m*:\n        # dy_i/dx_j = sech2_i * [delta_ij + J * sech2_j / (n * denom)]\n        # => grad_x_j = sech2_j * (grad_y_j + J * G / (n * denom))\n        # where G = sum_i(grad_y_i * sech2_i) per batch item (over neuron dim)\n        G = (grad_y * sech2).sum(dim=1, keepdim=True)   # (B,1,...)\n        grad_x = sech2 * (grad_y + J * G / (n * denom))\n\n        # grad wrt J_raw (chain through sigmoid: dJ/dJ_raw = J*(1-J))\n        # dy_i/dJ = sech2_i * m* / denom (IFT result)\n        grad_J = (grad_y * sech2 * m_star / denom).sum()\n        grad_J_raw = grad_J * J * (1.0 - J)\n\n        return grad_x, grad_J_raw, None  # K_max has no grad\n\n\nclass CWA(nn.Module):\n    \"\"\"\n    Curie-Weiss Activation: y_i = tanh(x_i + J * mean_neurons(y))\n    where J = sigmoid(J_raw) in (0,1) is a per-layer learnable scalar.\n\n    Hybrid backprop:\n      - J*s_bar < 0.8: unrolled autograd (few iters, exact, modest memory)\n      - J*s_bar >= 0.8: IFT backward via CWAFunction (O(1) activation memory)\n    \"\"\"\n    def __init__(self, K_max=50):\n        super().__init__()\n        self.J_raw = nn.Parameter(torch.zeros(1))  # J = sigmoid(0) = 0.5 at init\n        self.K_max = K_max\n        # Per-forward-call statistics (for logging)\n        self.last_J = None\n        self.last_J_s_bar = None\n        self.last_k = None\n        self.last_mode = None\n\n    def forward(self, x):\n        J = torch.sigmoid(self.J_raw)\n\n        # --- Step 1: probe J*s_bar with a no_grad fixed-point run ---\n        with torch.no_grad():\n            m_probe = torch.zeros(x.shape[0], 1, *x.shape[2:],\n                                  device=x.device, dtype=x.dtype)\n            for _ in range(self.K_max):\n                m_probe_new = torch.tanh(x + J * m_probe).mean(dim=1, keepdim=True)\n                if (m_probe_new - m_probe).abs().max() < 1e-5:\n                    m_probe = m_probe_new\n                    break\n                m_probe = m_probe_new\n            sech2_probe = 1.0 / torch.cosh(x + J * m_probe) ** 2\n            J_s_bar_probe = (J * sech2_probe.mean()).item()\n\n        use_ift = (J_s_bar_probe >= 0.8)\n\n        if use_ift:\n            # --- IFT MODE: custom backward, O(1) activation memory ---\n            y, J_s_bar_t, k_t = CWAFunction.apply(x, self.J_raw, self.K_max)\n            mode = 'IFT'\n        else:\n            # --- UNROLLED MODE: autograd tracks through K iterations ---\n            # K is small (< ~10) when J*s_bar < 0.8\n            m = torch.zeros(x.shape[0], 1, *x.shape[2:],\n                            device=x.device, dtype=x.dtype)\n            k = 0\n            for k in range(self.K_max):\n                h = x + J * m\n                m_new = torch.tanh(h).mean(dim=1, keepdim=True)\n                with torch.no_grad():\n                    diff = (m_new - m).abs().max()\n                    sech2_tmp = 1.0 / torch.cosh(h)**2\n                    s_tmp = sech2_tmp.mean()\n                    J_s_tmp = J * s_tmp\n                    delta = 1e-4 * (1.0 - J_s_tmp.clamp(max=0.9999))\n                m = m_new\n                if diff < delta:\n                    break\n            y = torch.tanh(x + J * m)\n            with torch.no_grad():\n                sech2_f = 1.0 / torch.cosh(x + J * m.detach())**2\n                J_s_bar_t = J * sech2_f.mean()\n            k_t = torch.tensor(float(k + 1))\n            mode = 'unrolled'\n\n        # Update logging state\n        self.last_J = J.item()\n        self.last_J_s_bar = J_s_bar_t.item() if torch.is_tensor(J_s_bar_t) else J_s_bar_t\n        self.last_k = k_t.item() if torch.is_tensor(k_t) else k_t\n        self.last_mode = mode\n        return y\n\n\n###############################################\n# resnet20.py — ResNet-20 with pluggable activation\n###############################################\n# Reference: He et al. 2016, akamaster/pytorch_resnet_cifar10 structure\n#\n# Standard ResNet-20: n_blocks=[3,3,3], widths=[16,32,64]\n# Wide-4x ResNet-20:  n_blocks=[3,3,3], widths=[64,128,256]\n# activation_fn: callable (no args) returning an nn.Module instance\n# use_bn: bool (True/False)\n\nimport torch.nn as nn\nimport torch.nn.functional as F\n\ndef make_act(act_name):\n    \"\"\"Factory for activations by name.\"\"\"\n    if act_name == 'CWA':    return CWA()\n    if act_name == 'GELU':   return nn.GELU()\n    if act_name == 'SELU':   return nn.SELU()\n    if act_name == 'ReLU':   return nn.ReLU()\n    if act_name == 'tanhLN': return nn.Sequential(nn.Tanh(), nn.LayerNorm(...))\n    if act_name == 'GELULN': return nn.Sequential(nn.GELU(), nn.LayerNorm(...))\n    raise ValueError(act_name)\n\n# NOTE: LayerNorm in BasicBlock for conv tensors must normalize over channel dim.\n# Use nn.GroupNorm(1, C) which is identical to LayerNorm over channels.\n# tanh+LN baseline = nn.Sequential(nn.Tanh(), nn.GroupNorm(1, C))\n# GELU+LN baseline = nn.Sequential(nn.GELU(), nn.GroupNorm(1, C))\n# These must be instantiated per-block with the correct channel count C.\n\nclass BasicBlock(nn.Module):\n    def __init__(self, in_planes, planes, stride, act_name, use_bn):\n        super().__init__()\n        self.conv1 = nn.Conv2d(in_planes, planes, 3, stride=stride, padding=1, bias=not use_bn)\n        self.bn1 = nn.BatchNorm2d(planes) if use_bn else nn.Identity()\n        self.conv2 = nn.Conv2d(planes, planes, 3, stride=1, padding=1, bias=not use_bn)\n        self.bn2 = nn.BatchNorm2d(planes) if use_bn else nn.Identity()\n\n        # Instantiate activations — each block has its OWN CWA instance (own J_raw)\n        self.act1 = self._make_act(act_name, planes)\n        self.act2 = self._make_act(act_name, planes)  # post-addition activation\n\n        # Shortcut: 1x1 conv if stride > 1 or channels change\n        self.shortcut = nn.Sequential()\n        if stride != 1 or in_planes != planes:\n            layers = [nn.Conv2d(in_planes, planes, 1, stride=stride, bias=not use_bn)]\n            if use_bn:\n                layers.append(nn.BatchNorm2d(planes))\n            self.shortcut = nn.Sequential(*layers)\n\n    def _make_act(self, act_name, planes):\n        if act_name == 'CWA':    return CWA()\n        if act_name == 'GELU':   return nn.GELU()\n        if act_name == 'SELU':   return nn.SELU()\n        if act_name == 'ReLU':   return nn.ReLU()\n        if act_name == 'tanhLN': return nn.Sequential(nn.Tanh(), nn.GroupNorm(1, planes))\n        if act_name == 'GELULN': return nn.Sequential(nn.GELU(), nn.GroupNorm(1, planes))\n        raise ValueError(act_name)\n\n    def forward(self, x):\n        out = self.act1(self.bn1(self.conv1(x)))\n        out = self.bn2(self.conv2(out))\n        out = out + self.shortcut(x)\n        out = self.act2(out)\n        return out\n\n\nclass ResNet20(nn.Module):\n    def __init__(self, widths=[16, 32, 64], n_blocks_per_group=3,\n                 num_classes=100, act_name='GELU', use_bn=False):\n        super().__init__()\n        self.act_name = act_name\n        self.use_bn = use_bn\n\n        # Initial conv\n        self.conv0 = nn.Conv2d(3, widths[0], 3, padding=1, bias=not use_bn)\n        self.bn0 = nn.BatchNorm2d(widths[0]) if use_bn else nn.Identity()\n        self.act0 = self._make_act(widths[0])\n\n        # 3 groups of residual blocks\n        self.group1 = self._make_group(widths[0], widths[0], n_blocks_per_group, stride=1)\n        self.group2 = self._make_group(widths[0], widths[1], n_blocks_per_group, stride=2)\n        self.group3 = self._make_group(widths[1], widths[2], n_blocks_per_group, stride=2)\n\n        # Classifier\n        self.pool = nn.AdaptiveAvgPool2d(1)\n        self.fc = nn.Linear(widths[2], num_classes)\n\n        # Weight init\n        for m in self.modules():\n            if isinstance(m, nn.Conv2d):\n                nn.init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')\n            elif isinstance(m, nn.Linear):\n                nn.init.xavier_uniform_(m.weight)\n\n    def _make_act(self, planes):\n        # same as BasicBlock._make_act\n        ...\n\n    def _make_group(self, in_planes, planes, n_blocks, stride):\n        blocks = [BasicBlock(in_planes, planes, stride, self.act_name, self.use_bn)]\n        for _ in range(n_blocks - 1):\n            blocks.append(BasicBlock(planes, planes, 1, self.act_name, self.use_bn))\n        return nn.Sequential(*blocks)\n\n    def forward(self, x):\n        out = self.act0(self.bn0(self.conv0(x)))\n        out = self.group1(out)\n        out = self.group2(out)\n        out = self.group3(out)\n        out = self.pool(out).flatten(1)\n        return self.fc(out)\n\n    def collect_cwa_stats(self):\n        \"\"\"\n        Iterate all CWA modules, collect (name, J, J_s_bar, k, mode).\n        Call AFTER a forward pass to get the most recent stats.\n        Group by residual block index for per-block analysis.\n        \"\"\"\n        stats = []\n        for name, module in self.named_modules():\n            if isinstance(module, CWA):\n                stats.append({\n                    'layer': name,\n                    'J': module.last_J,\n                    'J_s_bar': module.last_J_s_bar,\n                    'k': module.last_k,\n                    'mode': module.last_mode,\n                })\n        return stats\n\n\n###############################################\n# train_cifar.py — TRAINING LOOP\n###############################################\n\ndef get_cifar100_loaders(batch_size=128, data_dir='./data'):\n    # torchvision CIFAR-100; download=True\n    # Train aug: RandomCrop(32, padding=4), RandomHorizontalFlip, Normalize\n    # Test: Normalize only\n    # Normalize with CIFAR-100 mean/std:\n    #   mean=(0.5071, 0.4867, 0.4408), std=(0.2675, 0.2565, 0.2761)\n    ...\n\ndef train_one_config(act_name, widths, use_bn, num_classes=100, epochs=100,\n                     lr=None, seed=0, device='cuda'):\n    \"\"\"\n    Train ResNet-20 variant.\n    lr: if None, auto-select: use_bn=True→0.1, False→0.01 (no-BN needs lower lr)\n    Returns: dict with keys:\n      test_acc_per_epoch: list[float]\n      final_test_acc: float\n      per_block_J_s_bar_history: {block_name: list[float]}  # epoch averages\n      train_time_sec: float\n    \"\"\"\n    torch.manual_seed(seed)\n    if lr is None:\n        lr = 0.1 if use_bn else 0.01\n\n    model = ResNet20(widths=widths, act_name=act_name, use_bn=use_bn)\n    model = model.to(device)\n\n    optimizer = torch.optim.SGD(model.parameters(), lr=lr,\n                                momentum=0.9, weight_decay=5e-4)\n    scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=epochs)\n    criterion = nn.CrossEntropyLoss()\n\n    train_loader, test_loader = get_cifar100_loaders()\n\n    per_block_J_s_bar_history = {}  # name -> list of per-epoch averages\n    test_acc_per_epoch = []\n\n    for epoch in range(epochs):\n        model.train()\n        epoch_block_J_s_bar = {}  # accumulate across batches\n        n_batches = 0\n\n        for x, y in train_loader:\n            x, y = x.to(device), y.to(device)\n            optimizer.zero_grad()\n            logits = model(x)\n            loss = criterion(logits, y)\n            loss.backward()\n\n            # Gradient clipping for no-BN variant\n            if not use_bn:\n                torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n\n            optimizer.step()\n\n            # Collect per-block CWA stats AFTER each forward pass\n            if act_name == 'CWA':\n                stats = model.collect_cwa_stats()\n                for s in stats:\n                    if s['layer'] not in epoch_block_J_s_bar:\n                        epoch_block_J_s_bar[s['layer']] = []\n                    if s['J_s_bar'] is not None:\n                        epoch_block_J_s_bar[s['layer']].append(s['J_s_bar'])\n            n_batches += 1\n\n        scheduler.step()\n\n        # Epoch-average J*s_bar per block\n        if act_name == 'CWA':\n            for name, vals in epoch_block_J_s_bar.items():\n                if name not in per_block_J_s_bar_history:\n                    per_block_J_s_bar_history[name] = []\n                per_block_J_s_bar_history[name].append(float(sum(vals)/len(vals)) if vals else 0.0)\n\n        # Test accuracy\n        test_acc = evaluate(model, test_loader, device)\n        test_acc_per_epoch.append(test_acc)\n        if epoch % 10 == 0:\n            print(f'Ep {epoch}: test_acc={test_acc:.4f}')\n\n    return {\n        'final_test_acc': test_acc_per_epoch[-1],\n        'test_acc_per_epoch': test_acc_per_epoch,\n        'per_block_J_s_bar_history': per_block_J_s_bar_history,\n    }\n\n\ndef evaluate(model, loader, device):\n    model.eval()\n    correct, total = 0, 0\n    with torch.no_grad():\n        for x, y in loader:\n            x, y = x.to(device), y.to(device)\n            correct += (model(x).argmax(1) == y).sum().item()\n            total += y.size(0)\n    return correct / total\n\n\n###############################################\n# EXPERIMENT 2 MAIN LOOP\n###############################################\n\nEXPERIMENT_CONFIGS = [\n    # (label, widths, use_bn)\n    ('standard_no_bn',  [16, 32, 64],   False),\n    ('standard_bn',     [16, 32, 64],   True),\n    ('wide_no_bn',      [64, 128, 256], False),\n    ('wide_bn',         [64, 128, 256], True),\n]\n\n# Activations to compare per config:\n#   standard_no_bn: CWA, GELU, SELU, tanhLN, GELULN  (3 seeds for CWA+GELU, 1 seed others)\n#   standard_bn:    CWA, GELU  (3 seeds each)\n#   wide_no_bn:     CWA, GELU  (3 seeds each)\n#   wide_bn:        CWA, GELU  (3 seeds each)\n# Total: ~28 runs × ~8 min/run = ~3.5 hours (fits in 6h budget)\n\nACTIVATION_PLAN = {\n    'standard_no_bn':  [('CWA',3),('GELU',3),('SELU',1),('tanhLN',1),('GELULN',1)],\n    'standard_bn':     [('CWA',3),('GELU',3)],\n    'wide_no_bn':      [('CWA',3),('GELU',3)],\n    'wide_bn':         [('CWA',3),('GELU',3)],\n}\n\nresults = {}  # populated by run loop\n\nfor cfg_label, widths, use_bn in EXPERIMENT_CONFIGS:\n    results[cfg_label] = {}\n    for act_name, n_seeds in ACTIVATION_PLAN[cfg_label]:\n        seed_results = []\n        for seed in range(n_seeds):\n            r = train_one_config(act_name, widths, use_bn,\n                                 epochs=100, seed=seed)\n            seed_results.append(r)\n        # Aggregate\n        accs = [r['final_test_acc'] for r in seed_results]\n        results[cfg_label][act_name] = {\n            'test_acc_mean': float(sum(accs)/len(accs)),\n            'test_acc_std': float(torch.tensor(accs).std().item()) if len(accs)>1 else 0.0,\n            'seeds': accs,\n            # Per-block J*s_bar final-epoch average (CWA only)\n            'final_J_s_bar_per_block': {\n                name: [h[-1] for h in [r['per_block_J_s_bar_history'].get(name, [0.0]) for r in seed_results]]\n                for name in (seed_results[0]['per_block_J_s_bar_history'] if seed_results else {})\n            }\n        }\n\n\n###############################################\n# WIDTH CORRELATION ANALYSIS\n###############################################\n# After Exp2 runs, compute per-block accuracy gain of CWA vs GELU.\n# Use standard_no_bn (widths 16/32/64) and wide_no_bn (widths 64/128/256).\n# 'Per-block gain' proxy: final J*s_bar per block as metric of how much\n# CWA leveraged coupling, correlated with the block's channel width.\n\ndef compute_width_correlation(results):\n    width_correlation = {}\n    for cfg_label, widths, _ in EXPERIMENT_CONFIGS:\n        if cfg_label not in ('standard_no_bn', 'wide_no_bn'):\n            continue\n        cfg = results[cfg_label]\n        if 'CWA' not in cfg or 'GELU' not in cfg:\n            continue\n        cwa = cfg['CWA']\n        gelu = cfg['GELU']\n        overall_gain = cwa['test_acc_mean'] - gelu['test_acc_mean']\n\n        # Per-block J*s_bar final values (mean over seeds)\n        per_block_J_s_bar = {}\n        for block_name, vals_per_seed in cwa['final_J_s_bar_per_block'].items():\n            per_block_J_s_bar[block_name] = float(sum(vals_per_seed)/len(vals_per_seed))\n\n        # Map block names to channel width\n        # group1 blocks -> widths[0], group2 -> widths[1], group3 -> widths[2]\n        block_width_map = {}\n        for name in per_block_J_s_bar:\n            if 'group1' in name: block_width_map[name] = widths[0]\n            elif 'group2' in name: block_width_map[name] = widths[1]\n            elif 'group3' in name: block_width_map[name] = widths[2]\n\n        width_correlation[cfg_label] = {\n            'widths': widths,\n            'overall_cwa_gain_pct': overall_gain * 100,\n            'per_block_J_s_bar_final': per_block_J_s_bar,\n            'block_width_map': block_width_map,\n            # Correlation statistic: scipy.stats.pearsonr(widths_vals, J_s_bar_vals)\n        }\n    return width_correlation\n\n\n###############################################\n# overhead_bench.py — EXPERIMENT 5\n###############################################\n# Synthetic benchmark: batch=32, C=256, H=W=8 (mimics a ResNet mid-block)\n# Force J*s_bar to targets by: using x ~ N(0, sigma²) where sigma controls sech²\n# When x is small, sech²(x + J*m*) ≈ 1, so J*s_bar ≈ J.\n# Use x = torch.zeros + small noise (sigma=0.01) to pin J*s_bar ≈ J.\n# Target J*s_bar values: {0.1, 0.3, 0.5, 0.7, 0.8, 0.9, 0.95, 0.99}\n# For each target t: set J_raw = logit(t) = log(t/(1-t))  [clamp t to (0.01, 0.99)]\n\nimport time\nimport torch\n\nTARGET_J_S_BARS = [0.1, 0.3, 0.5, 0.7, 0.8, 0.9, 0.95, 0.99]\nN_WARMUP = 5\nN_TIMING = 20\nBATCH, C, H, W = 32, 256, 8, 8\n\ndef logit(t):\n    t = max(min(t, 0.99), 0.01)\n    return torch.tensor([math.log(t / (1 - t))], dtype=torch.float32)\n\ndef measure_cwa_overhead(device='cuda'):\n    overhead_table = []\n    gelu = nn.GELU().to(device)\n\n    for target in TARGET_J_S_BARS:\n        cwa = CWA(K_max=100).to(device)  # high K_max to allow convergence near criticality\n        # Force J via J_raw\n        with torch.no_grad():\n            cwa.J_raw.copy_(logit(target))\n        cwa.J_raw.requires_grad_(False)  # fix J for this measurement\n\n        # Use near-zero input so J*s_bar ≈ J\n        x = torch.randn(BATCH, C, H, W, device=device) * 0.01\n        x.requires_grad_(True)\n\n        # --- Warmup ---\n        for _ in range(N_WARMUP):\n            y = cwa(x)\n            y.sum().backward()\n            if x.grad is not None: x.grad.zero_()\n\n        # --- Measure CWA time ---\n        torch.cuda.synchronize()\n        times_cwa = []\n        for _ in range(N_TIMING):\n            torch.cuda.synchronize()\n            t0 = time.perf_counter()\n            y = cwa(x)\n            loss = y.sum()\n            loss.backward()\n            torch.cuda.synchronize()\n            times_cwa.append(time.perf_counter() - t0)\n            if x.grad is not None: x.grad.zero_()\n\n        # --- Measure CWA memory ---\n        torch.cuda.reset_peak_memory_stats(device)\n        y = cwa(x)\n        loss = y.sum()\n        loss.backward()\n        torch.cuda.synchronize()\n        mem_cwa_mb = torch.cuda.max_memory_allocated(device) / 1024**2\n\n        # Actual J*s_bar achieved (read from last forward pass)\n        actual_J_s_bar = cwa.last_J_s_bar\n        actual_k = cwa.last_k\n        actual_mode = cwa.last_mode\n\n        # --- Measure GELU baseline time ---\n        x_gelu = x.detach().requires_grad_(True)\n        for _ in range(N_WARMUP):\n            y_g = gelu(x_gelu)\n            y_g.sum().backward()\n            x_gelu.grad.zero_()\n\n        torch.cuda.synchronize()\n        times_gelu = []\n        for _ in range(N_TIMING):\n            torch.cuda.synchronize()\n            t0 = time.perf_counter()\n            y_g = gelu(x_gelu)\n            y_g.sum().backward()\n            torch.cuda.synchronize()\n            times_gelu.append(time.perf_counter() - t0)\n            x_gelu.grad.zero_()\n\n        # --- Measure GELU memory ---\n        torch.cuda.reset_peak_memory_stats(device)\n        y_g = gelu(x_gelu)\n        y_g.sum().backward()\n        torch.cuda.synchronize()\n        mem_gelu_mb = torch.cuda.max_memory_allocated(device) / 1024**2\n\n        wall_cwa = sum(times_cwa) / len(times_cwa) * 1000   # ms\n        wall_gelu = sum(times_gelu) / len(times_gelu) * 1000  # ms\n\n        overhead_table.append({\n            'J_s_bar_target': target,\n            'J_s_bar_actual': actual_J_s_bar,\n            'J_value': float(torch.sigmoid(cwa.J_raw).item()),\n            'K_star': actual_k,\n            'backprop_mode': actual_mode,\n            'wall_clock_ms_cwa': wall_cwa,\n            'wall_clock_ms_gelu': wall_gelu,\n            'wall_clock_ratio': wall_cwa / max(wall_gelu, 1e-9),\n            'memory_mb_cwa': mem_cwa_mb,\n            'memory_mb_gelu': mem_gelu_mb,\n            'memory_ratio': mem_cwa_mb / max(mem_gelu_mb, 1e-9),\n        })\n        print(f'J*s_bar={target}: K={actual_k}, mode={actual_mode}, '\n              f'wall={wall_cwa:.2f}ms/{wall_gelu:.2f}ms, '\n              f'mem={mem_cwa_mb:.1f}/{mem_gelu_mb:.1f}MB')\n\n    return overhead_table\n\n\n###############################################\n# method.py — ENTRY POINT & OUTPUT\n###############################################\nimport json\nfrom datetime import datetime\n\ndef compute_verdict(results, overhead_table, width_correlation):\n    \"\"\"\n    Evaluate success criteria:\n    1. memory_within_2x: all overhead_table entries have memory_ratio <= 2.0\n    2. width_positive_correlation: wider blocks have higher J*s_bar in CWA\n    3. cwa_vs_gelu_no_bn_significant: CWA > GELU on standard_no_bn\n    4. soc_observed: final J*s_bar mean across blocks > 0.7\n    \"\"\"\n    mem_ok = all(row['memory_ratio'] <= 2.0 for row in overhead_table)\n    # SOC: mean final J*s_bar across all blocks in standard_no_bn CWA run\n    cwa_blocks = results.get('standard_no_bn', {}).get('CWA', {}).get('final_J_s_bar_per_block', {})\n    mean_J_s_bar = 0.0\n    if cwa_blocks:\n        all_vals = [v for vals in cwa_blocks.values() for v in vals]\n        mean_J_s_bar = sum(all_vals) / max(len(all_vals), 1)\n    soc = mean_J_s_bar > 0.7\n    # Accuracy comparison\n    std_no_bn = results.get('standard_no_bn', {})\n    cwa_acc = std_no_bn.get('CWA', {}).get('test_acc_mean', 0)\n    gelu_acc = std_no_bn.get('GELU', {}).get('test_acc_mean', 0)\n    cwa_sig = (cwa_acc - gelu_acc) > 0.005  # >0.5% threshold\n    # Width correlation: check if group3 J*s_bar > group1 in wide_no_bn\n    wc = width_correlation.get('wide_no_bn', {})\n    J_s_by_width = {}\n    for block_name, J_s_bar_val in wc.get('per_block_J_s_bar_final', {}).items():\n        w = wc.get('block_width_map', {}).get(block_name, 0)\n        if w not in J_s_by_width: J_s_by_width[w] = []\n        J_s_by_width[w].append(J_s_bar_val)\n    sorted_widths = sorted(J_s_by_width.keys())\n    if len(sorted_widths) >= 2:\n        low_w_mean = sum(J_s_by_width[sorted_widths[0]]) / len(J_s_by_width[sorted_widths[0]])\n        high_w_mean = sum(J_s_by_width[sorted_widths[-1]]) / len(J_s_by_width[sorted_widths[-1]])\n        width_corr = (high_w_mean > low_w_mean)\n    else:\n        width_corr = None\n\n    return {\n        'memory_within_2x': mem_ok,\n        'cwa_vs_gelu_no_bn_significant': cwa_sig,\n        'soc_observed': soc,\n        'mean_final_J_s_bar': mean_J_s_bar,\n        'width_positive_correlation': width_corr,\n    }\n\ndef main():\n    import os\n    device = 'cuda' if torch.cuda.is_available() else 'cpu'\n    print(f'Device: {device}')\n\n    # ---- Experiment 2: ResNet-20 CIFAR-100 ----\n    print('=== Experiment 2: ResNet-20 CIFAR-100 ===')\n    # (run loop as described above)\n    all_results = run_all_resnet_configs(device=device)\n    width_correlation = compute_width_correlation(all_results)\n\n    # ---- Experiment 5: Overhead benchmark ----\n    print('=== Experiment 5: Computational overhead ===')\n    overhead_table = measure_cwa_overhead(device=device)\n\n    # ---- Verdict ----\n    verdict = compute_verdict(all_results, overhead_table, width_correlation)\n\n    # ---- Write output ----\n    out = {\n        'metadata': {\n            'timestamp': datetime.utcnow().isoformat(),\n            'device': device,\n            'epochs': 100,\n            'cifar100_classes': 100,\n            'n_seeds_main': 3,\n            'n_seeds_baselines': 1,\n        },\n        'resnet20_results': all_results,\n        'width_correlation': width_correlation,\n        'overhead_table': overhead_table,\n        'verdict': verdict,\n    }\n    with open('method_out.json', 'w') as f:\n        json.dump(out, f, indent=2)\n    print('Wrote method_out.json')\n\nif __name__ == '__main__':\n    main()\n\n\n###############################################\n# IMPORTANT IMPLEMENTATION NOTES FOR EXECUTOR\n###############################################\n# 1. CWA mean_neurons: ALWAYS average over dim=1 (channel dim) with keepdim=True.\n#    This is the 'neuron' dimension for both MLP (B,C) and conv (B,C,H,W) tensors.\n#    Spatial positions (H,W) are treated independently — each spatial location\n#    has its own mean-field mean over channels.\n#\n# 2. tanhLN and GELULN for conv tensors: use nn.GroupNorm(1, planes) which\n#    is equivalent to LayerNorm over the channel dimension. Standard\n#    nn.LayerNorm cannot be directly applied to (B,C,H,W) for channel-only norm.\n#\n# 3. No-BN training instability: use lr=0.01, grad clip max_norm=1.0, and\n#    Kaiming init. Expect GELU/ReLU to have HIGHER loss early in no-BN setting.\n#    CWA should be more stable here — this is the primary test.\n#\n# 4. CWA J_raw initialization: torch.zeros(1) → J=0.5. Each BasicBlock has\n#    TWO independent CWA instances (act1 post-conv1, act2 post-addition),\n#    each with their own J_raw. Track both in collect_cwa_stats().\n#\n# 5. Near-critical regime (J*s_bar=0.99): K_max may hit 100 without converging.\n#    This is expected and should be reported. DO NOT abort — run K_max iterations\n#    and note non-convergence in the overhead table.\n#\n# 6. Memory measurement for Exp5: call torch.cuda.reset_peak_memory_stats()\n#    IMMEDIATELY before the forward+backward pass, then read\n#    torch.cuda.max_memory_allocated() after. This gives net memory for that\n#    single pass, not cumulative. Use device='cuda:0' consistently.\n#\n# 7. IFT gradient denominator clamp: ALWAYS clamp denom=(1-J*s_bar) to min=1e-3\n#    to prevent division explosion if J*s_bar approaches 1 during training.\n#\n# 8. Total time budget: ~3.5h for Exp2, ~5 min for Exp5. If Exp2 is running\n#    long, reduce epochs to 50 and note in metadata. Priority: get all 4 configs\n#    (standard/wide × BN/no-BN) with at least 1 CWA seed and 1 GELU seed each.\n#    3 seeds are nice-to-have.\n#\n# 9. Checkpoint saves: after each config×activation×seed run, immediately\n#    append results to a partial_results.json file as backup against crashes.\n#\n# 10. CIFAR-100 normalization: mean=(0.5071, 0.4867, 0.4408),\n#     std=(0.2675, 0.2565, 0.2761). Use torchvision.datasets.CIFAR100 with\n#     download=True, root='./data'.",
  "fallback_plan": "## Fallback Scenarios and Mitigations\n\n### F1: CWA diverges in no-BN ResNet training\nSymptom: NaN loss within first 5 epochs.\nFix: Hard-clamp J_raw to max logit(0.95)=2.944 (i.e., J <= 0.95) by adding\n`self.J_raw.data.clamp_(max=2.944)` after each optimizer step. Also reduce\nbatch LR to 0.005 for no-BN configs. If still diverging, fall back to always\nusing IFT mode (safer gradients) and reducing K_max=20.\n\n### F2: IFT backward gives NaN or wrong gradients\nSymptom: loss oscillates wildly or CWA test acc worse than random.\nDiagnosis: Print grad norms per layer after first backward. If CWA layers have\nlarge grad norms, the IFT formula has a bug.\nFix A (quick): Switch CWA to always-unrolled mode (10 steps fixed, no stopping\ncriteria), bypassing CWAFunction entirely. Just run K=10 iterations through\nautograd. This is less memory-efficient but always correct.\nFix B: Verify IFT formula on a toy (n=4) example: compute numerical Jacobian\nand compare to IFT analytical Jacobian. Debug differences.\n\n### F3: Wide ResNet-20 OOM (20GB VRAM)\nSymptom: CUDA out of memory on wide (64/128/256) training.\nFix: Reduce batch size to 64 (wide has 4x channels, 4x more activations).\nIf still OOM, skip wide_bn variant (keep wide_no_bn as it's more important\nfor testing mean-field width prediction).\n\n### F4: Exp2 training takes too long (>5 hours)\nFix: Reduce epochs from 100 to 50. Priority order for what to keep:\n1. standard_no_bn: CWA(3 seeds), GELU(3 seeds) — MUST HAVE\n2. wide_no_bn: CWA(3 seeds), GELU(3 seeds) — MUST HAVE (for width analysis)\n3. standard_bn: CWA(1 seed), GELU(1 seed) — important for BN comparison\n4. wide_bn: CWA(1 seed), GELU(1 seed) — lowest priority\n5. SELU, tanhLN, GELULN baselines: 1 seed each, 50 epochs\n\n### F5: Overhead benchmark shows memory_ratio > 2x in IFT mode\nThis is a DISCONFIRM result per the hypothesis — report honestly.\nAdditionally: run unrolled-only CWA variant (bypass IFT regardless of J*s_bar)\nfor Exp5 to show the unrolled memory overhead grows with K_star.\nThis turns the disconfirm into a partial finding: IFT is needed for memory\ncontrol, and even IFT has > 2x overhead at very high J*s_bar (e.g., 0.99).\n\n### F6: J*s_bar never reaches above 0.5 during training (no SOC)\nReport as empirical finding: gradient incentive toward criticality is insufficient,\nJ stays near initialization (0.5). This disconfirms SOC but confirms CWA\nstill adds coupling (J ≠ 0). Add fixed-J ablation comparison:\nrun quick 1-seed 50-epoch experiment with J_raw frozen at logit(0.3), logit(0.5),\nlogit(0.7) to check if higher fixed J helps without SOC dynamics.\n\n### F7: torchvision CIFAR-100 download fails\nUse alternative: cached local copy or manual wget from\nhttps://www.cs.toronto.edu/~kriz/cifar-100-python.tar.gz and manual loading\nvia pickle. Implementation of manual loader is standard (see torchvision source).",
  "testing_plan": "## Testing Plan (Mini → Full Scale)\n\n### T0: Unit test CWA forward pass (< 1 minute)\n```python\n# Test 1: Fixed point convergence on (1, 8) tensor\nx = torch.randn(1, 8)\ncwa = CWA()\ny = cwa(x)\nassert y.shape == x.shape, 'Output shape mismatch'\nassert not torch.isnan(y).any(), 'NaN in CWA output'\nassert (y.abs() <= 1.0 + 1e-4).all(), 'tanh output must be in [-1,1]'\nprint(f'CWA test1: J={cwa.last_J:.3f}, J*s_bar={cwa.last_J_s_bar:.3f}, k={cwa.last_k}')\n\n# Test 2: Gradient flows\nx = torch.randn(4, 16, requires_grad=True)\ny = cwa(x)\nloss = y.sum()\nloss.backward()\nassert x.grad is not None, 'No gradient for x'\nassert cwa.J_raw.grad is not None, 'No gradient for J_raw'\nassert not torch.isnan(x.grad).any(), 'NaN in x.grad'\nassert not torch.isnan(cwa.J_raw.grad).any(), 'NaN in J_raw.grad'\n\n# Test 3: IFT mode activates when J forced high\ncwa_high = CWA()\nwith torch.no_grad():\n    cwa_high.J_raw.fill_(4.0)  # J = sigmoid(4) = 0.982\nx_small = torch.randn(4, 64) * 0.01  # near-zero to get J*s_bar ≈ J\ny_high = cwa_high(x_small)\nassert cwa_high.last_mode == 'IFT', f'Expected IFT, got {cwa_high.last_mode}'\n\n# Test 4: Unrolled mode when J is small\ncwa_low = CWA()\nwith torch.no_grad():\n    cwa_low.J_raw.fill_(-2.0)  # J = sigmoid(-2) = 0.12\ny_low = cwa_low(torch.randn(4, 64) * 0.01)\nassert cwa_low.last_mode == 'unrolled', f'Expected unrolled, got {cwa_low.last_mode}'\n```\n\n### T1: Mini ResNet-20 smoke test (< 5 minutes)\n```python\n# Create tiny model, run 2 batches, check loss decreases\nmodel = ResNet20(widths=[16,32,64], act_name='CWA', use_bn=False)\nx = torch.randn(8, 3, 32, 32)\ny = torch.randint(0, 100, (8,))\noptimizer = torch.optim.SGD(model.parameters(), lr=0.01)\nlosses = []\nfor _ in range(5):\n    optimizer.zero_grad()\n    loss = nn.CrossEntropyLoss()(model(x), y)\n    loss.backward()\n    torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)\n    optimizer.step()\n    losses.append(loss.item())\nprint('Mini smoke test losses:', losses)\n# Check no NaN; loss should change (not necessarily decrease on 8 samples)\nassert all(not math.isnan(l) for l in losses), 'NaN loss in smoke test'\n# Check CWA stats are being logged\nstats = model.collect_cwa_stats()\nassert len(stats) > 0, 'No CWA stats logged'\nprint(f'n_cwa_layers={len(stats)}, example J_s_bar={stats[0][\"J_s_bar\"]:.3f}')\n```\n\n### T2: CIFAR-100 mini-epoch sanity check (< 10 minutes)\n```python\n# Run 3 epochs on CIFAR-100 with batch_size=128 for CWA and GELU\n# Both on standard_no_bn config. Confirm:\n# - No NaN loss\n# - CWA loss <= GELU loss + 0.5 (CWA should be competitive within noise)\n# - J*s_bar history is being logged correctly\n# - 3 epochs should show loss decreasing from epoch 1 to 3\nfor act in ['CWA', 'GELU']:\n    r = train_one_config(act, [16,32,64], use_bn=False, epochs=3, seed=0)\n    print(f'{act}: epoch accs={r[\"test_acc_per_epoch\"]}')\n    assert all(not math.isnan(a) for a in r['test_acc_per_epoch'])\n```\n\n### T3: Overhead benchmark mini-check (< 5 minutes)\n```python\n# Run Exp5 for just 3 J*s_bar values: 0.1, 0.5, 0.9\n# Confirm:\n# - wall_clock_ratio is in a reasonable range (0.1x to 20x)\n# - memory_ratio is computed without errors\n# - backprop_mode switches from 'unrolled' to 'IFT' between 0.5 and 0.9\nfor target in [0.1, 0.5, 0.9]:\n    # run overhead measurement for just this target\n    ...\n```\n\n### T4: Confirmation signals before full-scale run\nAfter T2, check:\n1. Does J*s_bar evolve during 3 epochs? (Should change from ~0.5 init)\n2. Is CWA test accuracy after 3 epochs > 1% (i.e., better than pure chance @ 100 classes)?\n3. Are gradients flowing to J_raw (is it changing)?\nIf YES to all 3, proceed to full-scale Exp2.\n\n### T5: Progressive scale-up for Exp2\n- Step 1: Run standard_no_bn, CWA vs GELU, 1 seed, 20 epochs. Estimate time/epoch.\n- Step 2: Project to 100 epochs. If 100 epochs fits in remaining time, proceed.\n- Step 3: Run all 4 configs (standard/wide × BN/no-BN) in parallel if GPU permits\n  (only one model trains at a time; no multi-GPU needed).\n- Step 4: After each config×seed completes, immediately save partial results to\n  partial_results.json to protect against crashes."
}
````

### [21] TOOL RESULT — Write · 2026-06-16 17:31:06 UTC

````
{"type": "create", "filePath": "/ai-inventor/aii_data/runs/run_6gT5lHFn8559/3_invention_loop/iter_1/gen_plan/gen_plan_experiment_2/.terminal_claude_agent_struct_out.json", "content": "{\n  \"title\": \"CWA ResNet-20 CIFAR-100 Width Analysis + Computational Overhead Benchmark (Exp 2 & 5)\",\n  \"summary\": \"Implement CWA (Curie-Weiss Activation) as a custom PyTorch nn.Module with hybrid IFT/unrolled backprop. Run ResNet-20 on CIFAR-100 in standard (16/32/64) and wide-4x (64/128/256) variants, with and without BatchNorm, comparing CWA vs GELU/SELU/tanh+LN/GELU+LN over 3 seeds. Track per-block J*s_bar and compute CWA accuracy gain vs block width to test the mean-field large-n prediction. Separately run a synthetic overhead benchmark to measure wall-clock ratio and memory ratio CWA/GELU as a function of forced J*s_bar ∈ {0.1,0.3,0.5,0.7,0.8,0.9,0.95,0.99}.\",\n  \"runpod_compute_profile\": \"gpu\",\n  \"implementation_pseudocode\": \"###############################################\\n# FILE STRUCTURE\\n###############################################\\n# method.py          — main entry point, runs Exp2 + Exp5, writes method_out.json\\n# cwa.py             — CWAFunction (torch.autograd.Function) + CWA (nn.Module)\\n# resnet20.py        — ResNet-20 with pluggable activation, BN on/off\\n# train_cifar.py     — training loop, data loaders, per-block J*s_bar logging\\n# overhead_bench.py  — Exp5 synthetic benchmark\\n\\n###############################################\\n# cwa.py — CORE CWA IMPLEMENTATION\\n###############################################\\n\\nimport torch\\nimport torch.nn as nn\\n\\nclass CWAFunction(torch.autograd.Function):\\n    \\\"\\\"\\\"\\n    IFT-based custom backward for CWA (used when J*s_bar >= 0.8).\\n    Gradient bias is O(1e-4) uniformly due to adaptive tolerance delta.\\n    \\\"\\\"\\\"\\n    @staticmethod\\n    def forward(ctx, x, J_raw, K_max=50):\\n        # J in (0,1) via sigmoid\\n        J = torch.sigmoid(J_raw)  # scalar\\n        n = x.shape[1]            # number of neurons/channels\\n        extra = x.shape[2:]       # () for MLP, (H,W) for conv\\n\\n        # Fixed-point iteration: m_{t+1} = mean_neurons(tanh(x + J*m_t))\\n        # m is scalar-per-sample mean over dim=1\\n        m = torch.zeros(x.shape[0], 1, *extra, device=x.device, dtype=x.dtype)\\n        k_final = 0\\n        for k in range(K_max):\\n            h = x + J * m\\n            tanh_h = torch.tanh(h)\\n            m_new = tanh_h.mean(dim=1, keepdim=True)   # (B,1,...)\\n\\n            # Convergence check: adaptive tolerance\\n            with torch.no_grad():\\n                sech2_tmp = 1.0 / torch.cosh(h)**2\\n                s_bar_tmp = sech2_tmp.mean()\\n                J_s_bar_tmp = J * s_bar_tmp\\n                # clamp to avoid division near 1\\n                delta = 1e-4 * (1.0 - J_s_bar_tmp.clamp(max=0.9999))\\n\\n            diff = (m_new - m).abs().max()\\n            m = m_new\\n            k_final = k + 1\\n            if diff < delta:\\n                break\\n\\n        # Final values at m*\\n        h_star = x + J * m                          # (B,C,...)\\n        sech2 = 1.0 / torch.cosh(h_star) ** 2       # (B,C,...)\\n        s_bar = sech2.mean()                         # scalar\\n        J_s_bar = J * s_bar                          # scalar\\n        y = torch.tanh(h_star)                       # output\\n\\n        ctx.save_for_backward(x, m, J_raw, sech2, s_bar, J_s_bar)\\n        ctx.n = n\\n        ctx.k_final = k_final\\n        ctx.J_s_bar_val = J_s_bar.item()\\n\\n        return y, J_s_bar.detach(), torch.tensor(float(k_final), device=x.device)\\n\\n    @staticmethod\\n    def backward(ctx, grad_y, _g1, _g2):\\n        x, m_star, J_raw, sech2, s_bar, J_s_bar = ctx.saved_tensors\\n        J = torch.sigmoid(J_raw)\\n        n = ctx.n\\n\\n        # Safety clamp: prevent blow-up if J*s_bar drifts close to 1.0\\n        denom = (1.0 - J_s_bar).clamp(min=1e-3)\\n\\n        # Full IFT gradient accounting for inter-neuron coupling through m*:\\n        # dy_i/dx_j = sech2_i * [delta_ij + J * sech2_j / (n * denom)]\\n        # => grad_x_j = sech2_j * (grad_y_j + J * G / (n * denom))\\n        # where G = sum_i(grad_y_i * sech2_i) per batch item (over neuron dim)\\n        G = (grad_y * sech2).sum(dim=1, keepdim=True)   # (B,1,...)\\n        grad_x = sech2 * (grad_y + J * G / (n * denom))\\n\\n        # grad wrt J_raw (chain through sigmoid: dJ/dJ_raw = J*(1-J))\\n        # dy_i/dJ = sech2_i * m* / denom (IFT result)\\n        grad_J = (grad_y * sech2 * m_star / denom).sum()\\n        grad_J_raw = grad_J * J * (1.0 - J)\\n\\n        return grad_x, grad_J_raw, None  # K_max has no grad\\n\\n\\nclass CWA(nn.Module):\\n    \\\"\\\"\\\"\\n    Curie-Weiss Activation: y_i = tanh(x_i + J * mean_neurons(y))\\n    where J = sigmoid(J_raw) in (0,1) is a per-layer learnable scalar.\\n\\n    Hybrid backprop:\\n      - J*s_bar < 0.8: unrolled autograd (few iters, exact, modest memory)\\n      - J*s_bar >= 0.8: IFT backward via CWAFunction (O(1) activation memory)\\n    \\\"\\\"\\\"\\n    def __init__(self, K_max=50):\\n        super().__init__()\\n        self.J_raw = nn.Parameter(torch.zeros(1))  # J = sigmoid(0) = 0.5 at init\\n        self.K_max = K_max\\n        # Per-forward-call statistics (for logging)\\n        self.last_J = None\\n        self.last_J_s_bar = None\\n        self.last_k = None\\n        self.last_mode = None\\n\\n    def forward(self, x):\\n        J = torch.sigmoid(self.J_raw)\\n\\n        # --- Step 1: probe J*s_bar with a no_grad fixed-point run ---\\n        with torch.no_grad():\\n            m_probe = torch.zeros(x.shape[0], 1, *x.shape[2:],\\n                                  device=x.device, dtype=x.dtype)\\n            for _ in range(self.K_max):\\n                m_probe_new = torch.tanh(x + J * m_probe).mean(dim=1, keepdim=True)\\n                if (m_probe_new - m_probe).abs().max() < 1e-5:\\n                    m_probe = m_probe_new\\n                    break\\n                m_probe = m_probe_new\\n            sech2_probe = 1.0 / torch.cosh(x + J * m_probe) ** 2\\n            J_s_bar_probe = (J * sech2_probe.mean()).item()\\n\\n        use_ift = (J_s_bar_probe >= 0.8)\\n\\n        if use_ift:\\n            # --- IFT MODE: custom backward, O(1) activation memory ---\\n            y, J_s_bar_t, k_t = CWAFunction.apply(x, self.J_raw, self.K_max)\\n            mode = 'IFT'\\n        else:\\n            # --- UNROLLED MODE: autograd tracks through K iterations ---\\n            # K is small (< ~10) when J*s_bar < 0.8\\n            m = torch.zeros(x.shape[0], 1, *x.shape[2:],\\n                            device=x.device, dtype=x.dtype)\\n            k = 0\\n            for k in range(self.K_max):\\n                h = x + J * m\\n                m_new = torch.tanh(h).mean(dim=1, keepdim=True)\\n                with torch.no_grad():\\n                    diff = (m_new - m).abs().max()\\n                    sech2_tmp = 1.0 / torch.cosh(h)**2\\n                    s_tmp = sech2_tmp.mean()\\n                    J_s_tmp = J * s_tmp\\n                    delta = 1e-4 * (1.0 - J_s_tmp.clamp(max=0.9999))\\n                m = m_new\\n                if diff < delta:\\n                    break\\n            y = torch.tanh(x + J * m)\\n            with torch.no_grad():\\n                sech2_f = 1.0 / torch.cosh(x + J * m.detach())**2\\n                J_s_bar_t = J * sech2_f.mean()\\n            k_t = torch.tensor(float(k + 1))\\n            mode = 'unrolled'\\n\\n        # Update logging state\\n        self.last_J = J.item()\\n        self.last_J_s_bar = J_s_bar_t.item() if torch.is_tensor(J_s_bar_t) else J_s_bar_t\\n        self.last_k = k_t.item() if torch.is_tensor(k_t) else k_t\\n        self.last_mode = mode\\n        return y\\n\\n\\n###############################################\\n# resnet20.py — ResNet-20 with pluggable activation\\n###############################################\\n# Reference: He et al. 2016, akamaster/pytorch_resnet_cifar10 structure\\n#\\n# Standard ResNet-20: n_blocks=[3,3,3], widths=[16,32,64]\\n# Wide-4x ResNet-20:  n_blocks=[3,3,3], widths=[64,128,256]\\n# activation_fn: callable (no args) returning an nn.Module instance\\n# use_bn: bool (True/False)\\n\\nimport torch.nn as nn\\nimport torch.nn.functional as F\\n\\ndef make_act(act_name):\\n    \\\"\\\"\\\"Factory for activations by name.\\\"\\\"\\\"\\n    if act_name == 'CWA':    return CWA()\\n    if act_name == 'GELU':   return nn.GELU()\\n    if act_name == 'SELU':   return nn.SELU()\\n    if act_name == 'ReLU':   return nn.ReLU()\\n    if act_name == 'tanhLN': return nn.Sequential(nn.Tanh(), nn.LayerNorm(...))\\n    if act_name == 'GELULN': return nn.Sequential(nn.GELU(), nn.LayerNorm(...))\\n    raise ValueError(act_name)\\n\\n# NOTE: LayerNorm in BasicBlock for conv tensors must normalize over channel dim.\\n# Use nn.GroupNorm(1, C) which is identical to LayerNorm over channels.\\n# tanh+LN baseline = nn.Sequential(nn.Tanh(), nn.GroupNorm(1, C))\\n# GELU+LN baseline = nn.Sequential(nn.GELU(), nn.GroupNorm(1, C))\\n# These must be instantiated per-block with the correct channel count C.\\n\\nclass BasicBlock(nn.Module):\\n    def __init__(self, in_planes, planes, stride, act_name, use_bn):\\n        super().__init__()\\n        self.conv1 = nn.Conv2d(in_planes, planes, 3, stride=stride, padding=1, bias=not use_bn)\\n        self.bn1 = nn.BatchNorm2d(planes) if use_bn else nn.Identity()\\n        self.conv2 = nn.Conv2d(planes, planes, 3, stride=1, padding=1, bias=not use_bn)\\n        self.bn2 = nn.BatchNorm2d(planes) if use_bn else nn.Identity()\\n\\n        # Instantiate activations — each block has its OWN CWA instance (own J_raw)\\n        self.act1 = self._make_act(act_name, planes)\\n        self.act2 = self._make_act(act_name, planes)  # post-addition activation\\n\\n        # Shortcut: 1x1 conv if stride > 1 or channels change\\n        self.shortcut = nn.Sequential()\\n        if stride != 1 or in_planes != planes:\\n            layers = [nn.Conv2d(in_planes, planes, 1, stride=stride, bias=not use_bn)]\\n            if use_bn:\\n                layers.append(nn.BatchNorm2d(planes))\\n            self.shortcut = nn.Sequential(*layers)\\n\\n    def _make_act(self, act_name, planes):\\n        if act_name == 'CWA':    return CWA()\\n        if act_name == 'GELU':   return nn.GELU()\\n        if act_name == 'SELU':   return nn.SELU()\\n        if act_name == 'ReLU':   return nn.ReLU()\\n        if act_name == 'tanhLN': return nn.Sequential(nn.Tanh(), nn.GroupNorm(1, planes))\\n        if act_name == 'GELULN': return nn.Sequential(nn.GELU(), nn.GroupNorm(1, planes))\\n        raise ValueError(act_name)\\n\\n    def forward(self, x):\\n        out = self.act1(self.bn1(self.conv1(x)))\\n        out = self.bn2(self.conv2(out))\\n        out = out + self.shortcut(x)\\n        out = self.act2(out)\\n        return out\\n\\n\\nclass ResNet20(nn.Module):\\n    def __init__(self, widths=[16, 32, 64], n_blocks_per_group=3,\\n                 num_classes=100, act_name='GELU', use_bn=False):\\n        super().__init__()\\n        self.act_name = act_name\\n        self.use_bn = use_bn\\n\\n        # Initial conv\\n        self.conv0 = nn.Conv2d(3, widths[0], 3, padding=1, bias=not use_bn)\\n        self.bn0 = nn.BatchNorm2d(widths[0]) if use_bn else nn.Identity()\\n        self.act0 = self._make_act(widths[0])\\n\\n        # 3 groups of residual blocks\\n        self.group1 = self._make_group(widths[0], widths[0], n_blocks_per_group, stride=1)\\n        self.group2 = self._make_group(widths[0], widths[1], n_blocks_per_group, stride=2)\\n        self.group3 = self._make_group(widths[1], widths[2], n_blocks_per_group, stride=2)\\n\\n        # Classifier\\n        self.pool = nn.AdaptiveAvgPool2d(1)\\n        self.fc = nn.Linear(widths[2], num_classes)\\n\\n        # Weight init\\n        for m in self.modules():\\n            if isinstance(m, nn.Conv2d):\\n                nn.init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')\\n            elif isinstance(m, nn.Linear):\\n                nn.init.xavier_uniform_(m.weight)\\n\\n    def _make_act(self, planes):\\n        # same as BasicBlock._make_act\\n        ...\\n\\n    def _make_group(self, in_planes, planes, n_blocks, stride):\\n        blocks = [BasicBlock(in_planes, planes, stride, self.act_name, self.use_bn)]\\n        for _ in range(n_blocks - 1):\\n            blocks.append(BasicBlock(planes, planes, 1, self.act_name, self.use_bn))\\n        return nn.Sequential(*blocks)\\n\\n    def forward(self, x):\\n        out = self.act0(self.bn0(self.conv0(x)))\\n        out = self.group1(out)\\n        out = self.group2(out)\\n        out = self.group3(out)\\n        out = self.pool(out).flatten(1)\\n        return self.fc(out)\\n\\n    def collect_cwa_stats(self):\\n        \\\"\\\"\\\"\\n        Iterate all CWA modules, collect (name, J, J_s_bar, k, mode).\\n        Call AFTER a forward pass to get the most recent stats.\\n        Group by residual block index for per-block analysis.\\n        \\\"\\\"\\\"\\n        stats = []\\n        for name, module in self.named_modules():\\n            if isinstance(module, CWA):\\n                stats.append({\\n                    'layer': name,\\n                    'J': module.last_J,\\n                    'J_s_bar': module.last_J_s_bar,\\n                    'k': module.last_k,\\n                    'mode': module.last_mode,\\n                })\\n        return stats\\n\\n\\n###############################################\\n# train_cifar.py — TRAINING LOOP\\n###############################################\\n\\ndef get_cifar100_loaders(batch_size=128, data_dir='./data'):\\n    # torchvision CIFAR-100; download=True\\n    # Train aug: RandomCrop(32, padding=4), RandomHorizontalFlip, Normalize\\n    # Test: Normalize only\\n    # Normalize with CIFAR-100 mean/std:\\n    #   mean=(0.5071, 0.4867, 0.4408), std=(0.2675, 0.2565, 0.2761)\\n    ...\\n\\ndef train_one_config(act_name, widths, use_bn, num_classes=100, epochs=100,\\n                     lr=None, seed=0, device='cuda'):\\n    \\\"\\\"\\\"\\n    Train ResNet-20 variant.\\n    lr: if None, auto-select: use_bn=True→0.1, False→0.01 (no-BN needs lower lr)\\n    Returns: dict with keys:\\n      test_acc_per_epoch: list[float]\\n      final_test_acc: float\\n      per_block_J_s_bar_history: {block_name: list[float]}  # epoch averages\\n      train_time_sec: float\\n    \\\"\\\"\\\"\\n    torch.manual_seed(seed)\\n    if lr is None:\\n        lr = 0.1 if use_bn else 0.01\\n\\n    model = ResNet20(widths=widths, act_name=act_name, use_bn=use_bn)\\n    model = model.to(device)\\n\\n    optimizer = torch.optim.SGD(model.parameters(), lr=lr,\\n                                momentum=0.9, weight_decay=5e-4)\\n    scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=epochs)\\n    criterion = nn.CrossEntropyLoss()\\n\\n    train_loader, test_loader = get_cifar100_loaders()\\n\\n    per_block_J_s_bar_history = {}  # name -> list of per-epoch averages\\n    test_acc_per_epoch = []\\n\\n    for epoch in range(epochs):\\n        model.train()\\n        epoch_block_J_s_bar = {}  # accumulate across batches\\n        n_batches = 0\\n\\n        for x, y in train_loader:\\n            x, y = x.to(device), y.to(device)\\n            optimizer.zero_grad()\\n            logits = model(x)\\n            loss = criterion(logits, y)\\n            loss.backward()\\n\\n            # Gradient clipping for no-BN variant\\n            if not use_bn:\\n                torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\\n\\n            optimizer.step()\\n\\n            # Collect per-block CWA stats AFTER each forward pass\\n            if act_name == 'CWA':\\n                stats = model.collect_cwa_stats()\\n                for s in stats:\\n                    if s['layer'] not in epoch_block_J_s_bar:\\n                        epoch_block_J_s_bar[s['layer']] = []\\n                    if s['J_s_bar'] is not None:\\n                        epoch_block_J_s_bar[s['layer']].append(s['J_s_bar'])\\n            n_batches += 1\\n\\n        scheduler.step()\\n\\n        # Epoch-average J*s_bar per block\\n        if act_name == 'CWA':\\n            for name, vals in epoch_block_J_s_bar.items():\\n                if name not in per_block_J_s_bar_history:\\n                    per_block_J_s_bar_history[name] = []\\n                per_block_J_s_bar_history[name].append(float(sum(vals)/len(vals)) if vals else 0.0)\\n\\n        # Test accuracy\\n        test_acc = evaluate(model, test_loader, device)\\n        test_acc_per_epoch.append(test_acc)\\n        if epoch % 10 == 0:\\n            print(f'Ep {epoch}: test_acc={test_acc:.4f}')\\n\\n    return {\\n        'final_test_acc': test_acc_per_epoch[-1],\\n        'test_acc_per_epoch': test_acc_per_epoch,\\n        'per_block_J_s_bar_history': per_block_J_s_bar_history,\\n    }\\n\\n\\ndef evaluate(model, loader, device):\\n    model.eval()\\n    correct, total = 0, 0\\n    with torch.no_grad():\\n        for x, y in loader:\\n            x, y = x.to(device), y.to(device)\\n            correct += (model(x).argmax(1) == y).sum().item()\\n            total += y.size(0)\\n    return correct / total\\n\\n\\n###############################################\\n# EXPERIMENT 2 MAIN LOOP\\n###############################################\\n\\nEXPERIMENT_CONFIGS = [\\n    # (label, widths, use_bn)\\n    ('standard_no_bn',  [16, 32, 64],   False),\\n    ('standard_bn',     [16, 32, 64],   True),\\n    ('wide_no_bn',      [64, 128, 256], False),\\n    ('wide_bn',         [64, 128, 256], True),\\n]\\n\\n# Activations to compare per config:\\n#   standard_no_bn: CWA, GELU, SELU, tanhLN, GELULN  (3 seeds for CWA+GELU, 1 seed others)\\n#   standard_bn:    CWA, GELU  (3 seeds each)\\n#   wide_no_bn:     CWA, GELU  (3 seeds each)\\n#   wide_bn:        CWA, GELU  (3 seeds each)\\n# Total: ~28 runs × ~8 min/run = ~3.5 hours (fits in 6h budget)\\n\\nACTIVATION_PLAN = {\\n    'standard_no_bn':  [('CWA',3),('GELU',3),('SELU',1),('tanhLN',1),('GELULN',1)],\\n    'standard_bn':     [('CWA',3),('GELU',3)],\\n    'wide_no_bn':      [('CWA',3),('GELU',3)],\\n    'wide_bn':         [('CWA',3),('GELU',3)],\\n}\\n\\nresults = {}  # populated by run loop\\n\\nfor cfg_label, widths, use_bn in EXPERIMENT_CONFIGS:\\n    results[cfg_label] = {}\\n    for act_name, n_seeds in ACTIVATION_PLAN[cfg_label]:\\n        seed_results = []\\n        for seed in range(n_seeds):\\n            r = train_one_config(act_name, widths, use_bn,\\n                                 epochs=100, seed=seed)\\n            seed_results.append(r)\\n        # Aggregate\\n        accs = [r['final_test_acc'] for r in seed_results]\\n        results[cfg_label][act_name] = {\\n            'test_acc_mean': float(sum(accs)/len(accs)),\\n            'test_acc_std': float(torch.tensor(accs).std().item()) if len(accs)>1 else 0.0,\\n            'seeds': accs,\\n            # Per-block J*s_bar final-epoch average (CWA only)\\n            'final_J_s_bar_per_block': {\\n                name: [h[-1] for h in [r['per_block_J_s_bar_history'].get(name, [0.0]) for r in seed_results]]\\n                for name in (seed_results[0]['per_block_J_s_bar_history'] if seed_results else {})\\n            }\\n        }\\n\\n\\n###############################################\\n# WIDTH CORRELATION ANALYSIS\\n###############################################\\n# After Exp2 runs, compute per-block accuracy gain of CWA vs GELU.\\n# Use standard_no_bn (widths 16/32/64) and wide_no_bn (widths 64/128/256).\\n# 'Per-block gain' proxy: final J*s_bar per block as metric of how much\\n# CWA leveraged coupling, correlated with the block's channel width.\\n\\ndef compute_width_correlation(results):\\n    width_correlation = {}\\n    for cfg_label, widths, _ in EXPERIMENT_CONFIGS:\\n        if cfg_label not in ('standard_no_bn', 'wide_no_bn'):\\n            continue\\n        cfg = results[cfg_label]\\n        if 'CWA' not in cfg or 'GELU' not in cfg:\\n            continue\\n        cwa = cfg['CWA']\\n        gelu = cfg['GELU']\\n        overall_gain = cwa['test_acc_mean'] - gelu['test_acc_mean']\\n\\n        # Per-block J*s_bar final values (mean over seeds)\\n        per_block_J_s_bar = {}\\n        for block_name, vals_per_seed in cwa['final_J_s_bar_per_block'].items():\\n            per_block_J_s_bar[block_name] = float(sum(vals_per_seed)/len(vals_per_seed))\\n\\n        # Map block names to channel width\\n        # group1 blocks -> widths[0], group2 -> widths[1], group3 -> widths[2]\\n        block_width_map = {}\\n        for name in per_block_J_s_bar:\\n            if 'group1' in name: block_width_map[name] = widths[0]\\n            elif 'group2' in name: block_width_map[name] = widths[1]\\n            elif 'group3' in name: block_width_map[name] = widths[2]\\n\\n        width_correlation[cfg_label] = {\\n            'widths': widths,\\n            'overall_cwa_gain_pct': overall_gain * 100,\\n            'per_block_J_s_bar_final': per_block_J_s_bar,\\n            'block_width_map': block_width_map,\\n            # Correlation statistic: scipy.stats.pearsonr(widths_vals, J_s_bar_vals)\\n        }\\n    return width_correlation\\n\\n\\n###############################################\\n# overhead_bench.py — EXPERIMENT 5\\n###############################################\\n# Synthetic benchmark: batch=32, C=256, H=W=8 (mimics a ResNet mid-block)\\n# Force J*s_bar to targets by: using x ~ N(0, sigma²) where sigma controls sech²\\n# When x is small, sech²(x + J*m*) ≈ 1, so J*s_bar ≈ J.\\n# Use x = torch.zeros + small noise (sigma=0.01) to pin J*s_bar ≈ J.\\n# Target J*s_bar values: {0.1, 0.3, 0.5, 0.7, 0.8, 0.9, 0.95, 0.99}\\n# For each target t: set J_raw = logit(t) = log(t/(1-t))  [clamp t to (0.01, 0.99)]\\n\\nimport time\\nimport torch\\n\\nTARGET_J_S_BARS = [0.1, 0.3, 0.5, 0.7, 0.8, 0.9, 0.95, 0.99]\\nN_WARMUP = 5\\nN_TIMING = 20\\nBATCH, C, H, W = 32, 256, 8, 8\\n\\ndef logit(t):\\n    t = max(min(t, 0.99), 0.01)\\n    return torch.tensor([math.log(t / (1 - t))], dtype=torch.float32)\\n\\ndef measure_cwa_overhead(device='cuda'):\\n    overhead_table = []\\n    gelu = nn.GELU().to(device)\\n\\n    for target in TARGET_J_S_BARS:\\n        cwa = CWA(K_max=100).to(device)  # high K_max to allow convergence near criticality\\n        # Force J via J_raw\\n        with torch.no_grad():\\n            cwa.J_raw.copy_(logit(target))\\n        cwa.J_raw.requires_grad_(False)  # fix J for this measurement\\n\\n        # Use near-zero input so J*s_bar ≈ J\\n        x = torch.randn(BATCH, C, H, W, device=device) * 0.01\\n        x.requires_grad_(True)\\n\\n        # --- Warmup ---\\n        for _ in range(N_WARMUP):\\n            y = cwa(x)\\n            y.sum().backward()\\n            if x.grad is not None: x.grad.zero_()\\n\\n        # --- Measure CWA time ---\\n        torch.cuda.synchronize()\\n        times_cwa = []\\n        for _ in range(N_TIMING):\\n            torch.cuda.synchronize()\\n            t0 = time.perf_counter()\\n            y = cwa(x)\\n            loss = y.sum()\\n            loss.backward()\\n            torch.cuda.synchronize()\\n            times_cwa.append(time.perf_counter() - t0)\\n            if x.grad is not None: x.grad.zero_()\\n\\n        # --- Measure CWA memory ---\\n        torch.cuda.reset_peak_memory_stats(device)\\n        y = cwa(x)\\n        loss = y.sum()\\n        loss.backward()\\n        torch.cuda.synchronize()\\n        mem_cwa_mb = torch.cuda.max_memory_allocated(device) / 1024**2\\n\\n        # Actual J*s_bar achieved (read from last forward pass)\\n        actual_J_s_bar = cwa.last_J_s_bar\\n        actual_k = cwa.last_k\\n        actual_mode = cwa.last_mode\\n\\n        # --- Measure GELU baseline time ---\\n        x_gelu = x.detach().requires_grad_(True)\\n        for _ in range(N_WARMUP):\\n            y_g = gelu(x_gelu)\\n            y_g.sum().backward()\\n            x_gelu.grad.zero_()\\n\\n        torch.cuda.synchronize()\\n        times_gelu = []\\n        for _ in range(N_TIMING):\\n            torch.cuda.synchronize()\\n            t0 = time.perf_counter()\\n            y_g = gelu(x_gelu)\\n            y_g.sum().backward()\\n            torch.cuda.synchronize()\\n            times_gelu.append(time.perf_counter() - t0)\\n            x_gelu.grad.zero_()\\n\\n        # --- Measure GELU memory ---\\n        torch.cuda.reset_peak_memory_stats(device)\\n        y_g = gelu(x_gelu)\\n        y_g.sum().backward()\\n        torch.cuda.synchronize()\\n        mem_gelu_mb = torch.cuda.max_memory_allocated(device) / 1024**2\\n\\n        wall_cwa = sum(times_cwa) / len(times_cwa) * 1000   # ms\\n        wall_gelu = sum(times_gelu) / len(times_gelu) * 1000  # ms\\n\\n        overhead_table.append({\\n            'J_s_bar_target': target,\\n            'J_s_bar_actual': actual_J_s_bar,\\n            'J_value': float(torch.sigmoid(cwa.J_raw).item()),\\n            'K_star': actual_k,\\n            'backprop_mode': actual_mode,\\n            'wall_clock_ms_cwa': wall_cwa,\\n            'wall_clock_ms_gelu': wall_gelu,\\n            'wall_clock_ratio': wall_cwa / max(wall_gelu, 1e-9),\\n            'memory_mb_cwa': mem_cwa_mb,\\n            'memory_mb_gelu': mem_gelu_mb,\\n            'memory_ratio': mem_cwa_mb / max(mem_gelu_mb, 1e-9),\\n        })\\n        print(f'J*s_bar={target}: K={actual_k}, mode={actual_mode}, '\\n              f'wall={wall_cwa:.2f}ms/{wall_gelu:.2f}ms, '\\n              f'mem={mem_cwa_mb:.1f}/{mem_gelu_mb:.1f}MB')\\n\\n    return overhead_table\\n\\n\\n###############################################\\n# method.py — ENTRY POINT & OUTPUT\\n###############################################\\nimport json\\nfrom datetime import datetime\\n\\ndef compute_verdict(results, overhead_table, width_correlation):\\n    \\\"\\\"\\\"\\n    Evaluate success criteria:\\n    1. memory_within_2x: all overhead_table entries have memory_ratio <= 2.0\\n    2. width_positive_correlation: wider blocks have higher J*s_bar in CWA\\n    3. cwa_vs_gelu_no_bn_significant: CWA > GELU on standard_no_bn\\n    4. soc_observed: final J*s_bar mean across blocks > 0.7\\n    \\\"\\\"\\\"\\n    mem_ok = all(row['memory_ratio'] <= 2.0 for row in overhead_table)\\n    # SOC: mean final J*s_bar across all blocks in standard_no_bn CWA run\\n    cwa_blocks = results.get('standard_no_bn', {}).get('CWA', {}).get('final_J_s_bar_per_block', {})\\n    mean_J_s_bar = 0.0\\n    if cwa_blocks:\\n        all_vals = [v for vals in cwa_blocks.values() for v in vals]\\n        mean_J_s_bar = sum(all_vals) / max(len(all_vals), 1)\\n    soc = mean_J_s_bar > 0.7\\n    # Accuracy comparison\\n    std_no_bn = results.get('standard_no_bn', {})\\n    cwa_acc = std_no_bn.get('CWA', {}).get('test_acc_mean', 0)\\n    gelu_acc = std_no_bn.get('GELU', {}).get('test_acc_mean', 0)\\n    cwa_sig = (cwa_acc - gelu_acc) > 0.005  # >0.5% threshold\\n    # Width correlation: check if group3 J*s_bar > group1 in wide_no_bn\\n    wc = width_correlation.get('wide_no_bn', {})\\n    J_s_by_width = {}\\n    for block_name, J_s_bar_val in wc.get('per_block_J_s_bar_final', {}).items():\\n        w = wc.get('block_width_map', {}).get(block_name, 0)\\n        if w not in J_s_by_width: J_s_by_width[w] = []\\n        J_s_by_width[w].append(J_s_bar_val)\\n    sorted_widths = sorted(J_s_by_width.keys())\\n    if len(sorted_widths) >= 2:\\n        low_w_mean = sum(J_s_by_width[sorted_widths[0]]) / len(J_s_by_width[sorted_widths[0]])\\n        high_w_mean = sum(J_s_by_width[sorted_widths[-1]]) / len(J_s_by_width[sorted_widths[-1]])\\n        width_corr = (high_w_mean > low_w_mean)\\n    else:\\n        width_corr = None\\n\\n    return {\\n        'memory_within_2x': mem_ok,\\n        'cwa_vs_gelu_no_bn_significant': cwa_sig,\\n        'soc_observed': soc,\\n        'mean_final_J_s_bar': mean_J_s_bar,\\n        'width_positive_correlation': width_corr,\\n    }\\n\\ndef main():\\n    import os\\n    device = 'cuda' if torch.cuda.is_available() else 'cpu'\\n    print(f'Device: {device}')\\n\\n    # ---- Experiment 2: ResNet-20 CIFAR-100 ----\\n    print('=== Experiment 2: ResNet-20 CIFAR-100 ===')\\n    # (run loop as described above)\\n    all_results = run_all_resnet_configs(device=device)\\n    width_correlation = compute_width_correlation(all_results)\\n\\n    # ---- Experiment 5: Overhead benchmark ----\\n    print('=== Experiment 5: Computational overhead ===')\\n    overhead_table = measure_cwa_overhead(device=device)\\n\\n    # ---- Verdict ----\\n    verdict = compute_verdict(all_results, overhead_table, width_correlation)\\n\\n    # ---- Write output ----\\n    out = {\\n        'metadata': {\\n            'timestamp': datetime.utcnow().isoformat(),\\n            'device': device,\\n            'epochs': 100,\\n            'cifar100_classes': 100,\\n            'n_seeds_main': 3,\\n            'n_seeds_baselines': 1,\\n        },\\n        'resnet20_results': all_results,\\n        'width_correlation': width_correlation,\\n        'overhead_table': overhead_table,\\n        'verdict': verdict,\\n    }\\n    with open('method_out.json', 'w') as f:\\n        json.dump(out, f, indent=2)\\n    print('Wrote method_out.json')\\n\\nif __name__ == '__main__':\\n    main()\\n\\n\\n###############################################\\n# IMPORTANT IMPLEMENTATION NOTES FOR EXECUTOR\\n###############################################\\n# 1. CWA mean_neurons: ALWAYS average over dim=1 (channel dim) with keepdim=True.\\n#    This is the 'neuron' dimension for both MLP (B,C) and conv (B,C,H,W) tensors.\\n#    Spatial positions (H,W) are treated independently — each spatial location\\n#    has its own mean-field mean over channels.\\n#\\n# 2. tanhLN and GELULN for conv tensors: use nn.GroupNorm(1, planes) which\\n#    is equivalent to LayerNorm over the channel dimension. Standard\\n#    nn.LayerNorm cannot be directly applied to (B,C,H,W) for channel-only norm.\\n#\\n# 3. No-BN training instability: use lr=0.01, grad clip max_norm=1.0, and\\n#    Kaiming init. Expect GELU/ReLU to have HIGHER loss early in no-BN setting.\\n#    CWA should be more stable here — this is the primary test.\\n#\\n# 4. CWA J_raw initialization: torch.zeros(1) → J=0.5. Each BasicBlock has\\n#    TWO independent CWA instances (act1 post-conv1, act2 post-addition),\\n#    each with their own J_raw. Track both in collect_cwa_stats().\\n#\\n# 5. Near-critical regime (J*s_bar=0.99): K_max may hit 100 without converging.\\n#    This is expected and should be reported. DO NOT abort — run K_max iterations\\n#    and note non-convergence in the overhead table.\\n#\\n# 6. Memory measurement for Exp5: call torch.cuda.reset_peak_memory_stats()\\n#    IMMEDIATELY before the forward+backward pass, then read\\n#    torch.cuda.max_memory_allocated() after. This gives net memory for that\\n#    single pass, not cumulative. Use device='cuda:0' consistently.\\n#\\n# 7. IFT gradient denominator clamp: ALWAYS clamp denom=(1-J*s_bar) to min=1e-3\\n#    to prevent division explosion if J*s_bar approaches 1 during training.\\n#\\n# 8. Total time budget: ~3.5h for Exp2, ~5 min for Exp5. If Exp2 is running\\n#    long, reduce epochs to 50 and note in metadata. Priority: get all 4 configs\\n#    (standard/wide × BN/no-BN) with at least 1 CWA seed and 1 GELU seed each.\\n#    3 seeds are nice-to-have.\\n#\\n# 9. Checkpoint saves: after each config×activation×seed run, immediately\\n#    append results to a partial_results.json file as backup against crashes.\\n#\\n# 10. CIFAR-100 normalization: mean=(0.5071, 0.4867, 0.4408),\\n#     std=(0.2675, 0.2565, 0.2761). Use torchvision.datasets.CIFAR100 with\\n#     download=True, root='./data'.\",\n  \"fallback_plan\": \"## Fallback Scenarios and Mitigations\\n\\n### F1: CWA diverges in no-BN ResNet training\\nSymptom: NaN loss within first 5 epochs.\\nFix: Hard-clamp J_raw to max logit(0.95)=2.944 (i.e., J <= 0.95) by adding\\n`self.J_raw.data.clamp_(max=2.944)` after each optimizer step. Also reduce\\nbatch LR to 0.005 for no-BN configs. If still diverging, fall back to always\\nusing IFT mode (safer gradients) and reducing K_max=20.\\n\\n### F2: IFT backward gives NaN or wrong gradients\\nSymptom: loss oscillates wildly or CWA test acc worse than random.\\nDiagnosis: Print grad norms per layer after first backward. If CWA layers have\\nlarge grad norms, the IFT formula has a bug.\\nFix A (quick): Switch CWA to always-unrolled mode (10 steps fixed, no stopping\\ncriteria), bypassing CWAFunction entirely. Just run K=10 iterations through\\nautograd. This is less memory-efficient but always correct.\\nFix B: Verify IFT formula on a toy (n=4) example: compute numerical Jacobian\\nand compare to IFT analytical Jacobian. Debug differences.\\n\\n### F3: Wide ResNet-20 OOM (20GB VRAM)\\nSymptom: CUDA out of memory on wide (64/128/256) training.\\nFix: Reduce batch size to 64 (wide has 4x channels, 4x more activations).\\nIf still OOM, skip wide_bn variant (keep wide_no_bn as it's more important\\nfor testing mean-field width prediction).\\n\\n### F4: Exp2 training takes too long (>5 hours)\\nFix: Reduce epochs from 100 to 50. Priority order for what to keep:\\n1. standard_no_bn: CWA(3 seeds), GELU(3 seeds) — MUST HAVE\\n2. wide_no_bn: CWA(3 seeds), GELU(3 seeds) — MUST HAVE (for width analysis)\\n3. standard_bn: CWA(1 seed), GELU(1 seed) — important for BN comparison\\n4. wide_bn: CWA(1 seed), GELU(1 seed) — lowest priority\\n5. SELU, tanhLN, GELULN baselines: 1 seed each, 50 epochs\\n\\n### F5: Overhead benchmark shows memory_ratio > 2x in IFT mode\\nThis is a DISCONFIRM result per the hypothesis — report honestly.\\nAdditionally: run unrolled-only CWA variant (bypass IFT regardless of J*s_bar)\\nfor Exp5 to show the unrolled memory overhead grows with K_star.\\nThis turns the disconfirm into a partial finding: IFT is needed for memory\\ncontrol, and even IFT has > 2x overhead at very high J*s_bar (e.g., 0.99).\\n\\n### F6: J*s_bar never reaches above 0.5 during training (no SOC)\\nReport as empirical finding: gradient incentive toward criticality is insufficient,\\nJ stays near initialization (0.5). This disconfirms SOC but confirms CWA\\nstill adds coupling (J ≠ 0). Add fixed-J ablation comparison:\\nrun quick 1-seed 50-epoch experiment with J_raw frozen at logit(0.3), logit(0.5),\\nlogit(0.7) to check if higher fixed J helps without SOC dynamics.\\n\\n### F7: torchvision CIFAR-100 download fails\\nUse alternative: cached local copy or manual wget from\\nhttps://www.cs.toronto.edu/~kriz/cifar-100-python.tar.gz and manual loading\\nvia pickle. Implementation of manual loader is standard (see torchvision source).\",\n  \"testing_plan\": \"## Testing Plan (Mini → Full Scale)\\n\\n### T0: Unit test CWA forward pass (< 1 minute)\\n```python\\n# Test 1: Fixed point convergence on (1, 8) tensor\\nx = torch.randn(1, 8)\\ncwa = CWA()\\ny = cwa(x)\\nassert y.shape == x.shape, 'Output shape mismatch'\\nassert not torch.isnan(y).any(), 'NaN in CWA output'\\nassert (y.abs() <= 1.0 + 1e-4).all(), 'tanh output must be in [-1,1]'\\nprint(f'CWA test1: J={cwa.last_J:.3f}, J*s_bar={cwa.last_J_s_bar:.3f}, k={cwa.last_k}')\\n\\n# Test 2: Gradient flows\\nx = torch.randn(4, 16, requires_grad=True)\\ny = cwa(x)\\nloss = y.sum()\\nloss.backward()\\nassert x.grad is not None, 'No gradient for x'\\nassert cwa.J_raw.grad is not None, 'No gradient for J_raw'\\nassert not torch.isnan(x.grad).any(), 'NaN in x.grad'\\nassert not torch.isnan(cwa.J_raw.grad).any(), 'NaN in J_raw.grad'\\n\\n# Test 3: IFT mode activates when J forced high\\ncwa_high = CWA()\\nwith torch.no_grad():\\n    cwa_high.J_raw.fill_(4.0)  # J = sigmoid(4) = 0.982\\nx_small = torch.randn(4, 64) * 0.01  # near-zero to get J*s_bar ≈ J\\ny_high = cwa_high(x_small)\\nassert cwa_high.last_mode == 'IFT', f'Expected IFT, got {cwa_high.last_mode}'\\n\\n# Test 4: Unrolled mode when J is small\\ncwa_low = CWA()\\nwith torch.no_grad():\\n    cwa_low.J_raw.fill_(-2.0)  # J = sigmoid(-2) = 0.12\\ny_low = cwa_low(torch.randn(4, 64) * 0.01)\\nassert cwa_low.last_mode == 'unrolled', f'Expected unrolled, got {cwa_low.last_mode}'\\n```\\n\\n### T1: Mini ResNet-20 smoke test (< 5 minutes)\\n```python\\n# Create tiny model, run 2 batches, check loss decreases\\nmodel = ResNet20(widths=[16,32,64], act_name='CWA', use_bn=False)\\nx = torch.randn(8, 3, 32, 32)\\ny = torch.randint(0, 100, (8,))\\noptimizer = torch.optim.SGD(model.parameters(), lr=0.01)\\nlosses = []\\nfor _ in range(5):\\n    optimizer.zero_grad()\\n    loss = nn.CrossEntropyLoss()(model(x), y)\\n    loss.backward()\\n    torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)\\n    optimizer.step()\\n    losses.append(loss.item())\\nprint('Mini smoke test losses:', losses)\\n# Check no NaN; loss should change (not necessarily decrease on 8 samples)\\nassert all(not math.isnan(l) for l in losses), 'NaN loss in smoke test'\\n# Check CWA stats are being logged\\nstats = model.collect_cwa_stats()\\nassert len(stats) > 0, 'No CWA stats logged'\\nprint(f'n_cwa_layers={len(stats)}, example J_s_bar={stats[0][\\\"J_s_bar\\\"]:.3f}')\\n```\\n\\n### T2: CIFAR-100 mini-epoch sanity check (< 10 minutes)\\n```python\\n# Run 3 epochs on CIFAR-100 with batch_size=128 for CWA and GELU\\n# Both on standard_no_bn config. Confirm:\\n# - No NaN loss\\n# - CWA loss <= GELU loss + 0.5 (CWA should be competitive within noise)\\n# - J*s_bar history is being logged correctly\\n# - 3 epochs should show loss decreasing from epoch 1 to 3\\nfor act in ['CWA', 'GELU']:\\n    r = train_one_config(act, [16,32,64], use_bn=False, epochs=3, seed=0)\\n    print(f'{act}: epoch accs={r[\\\"test_acc_per_epoch\\\"]}')\\n    assert all(not math.isnan(a) for a in r['test_acc_per_epoch'])\\n```\\n\\n### T3: Overhead benchmark mini-check (< 5 minutes)\\n```python\\n# Run Exp5 for just 3 J*s_bar values: 0.1, 0.5, 0.9\\n# Confirm:\\n# - wall_clock_ratio is in a reasonable range (0.1x to 20x)\\n# - memory_ratio is computed without errors\\n# - backprop_mode switches from 'unrolled' to 'IFT' between 0.5 and 0.9\\nfor target in [0.1, 0.5, 0.9]:\\n    # run overhead measurement for just this target\\n    ...\\n```\\n\\n### T4: Confirmation signals before full-scale run\\nAfter T2, check:\\n1. Does J*s_bar evolve during 3 epochs? (Should change from ~0.5 init)\\n2. Is CWA test accuracy after 3 epochs > 1% (i.e., better than pure chance @ 100 classes)?\\n3. Are gradients flowing to J_raw (is it changing)?\\nIf YES to all 3, proceed to full-scale Exp2.\\n\\n### T5: Progressive scale-up for Exp2\\n- Step 1: Run standard_no_bn, CWA vs GELU, 1 seed, 20 epochs. Estimate time/epoch.\\n- Step 2: Project to 100 epochs. If 100 epochs fits in remaining time, proceed.\\n- Step 3: Run all 4 configs (standard/wide × BN/no-BN) in parallel if GPU permits\\n  (only one model trains at a time; no multi-GPU needed).\\n- Step 4: After each config×seed completes, immediately save partial results to\\n  partial_results.json to protect against crashes.\"\n}\n", "structuredPatch": [], "originalFile": null, "userModified": false}
````

### [22] ASSISTANT · 2026-06-16 17:31:36 UTC

```
Plan written to `.terminal_claude_agent_struct_out.json`. 

The plan covers:

- **CWA implementation**: `CWAFunction` (custom `torch.autograd.Function` with exact IFT backward including the inter-neuron coupling correction term `J·G/(n·denom)`), plus `CWA` `nn.Module` with the hybrid probe-then-switch logic (unrolled when J·s̄ < 0.8, IFT when ≥ 0.8)
- **ResNet-20**: Pluggable activation per block, standard (16/32/64) and wide-4× (64/128/256) variants, BN on/off. `GroupNorm(1, C)` for the tanhLN/GELULN baselines on conv tensors. Per-block `collect_cwa_stats()` for J·s̄ logging
- **Training**: SGD + cosine LR (lr=0.01 for no-BN, 0.1 for BN), grad clip 1.0 for no-BN, 100 epochs, 3 seeds for CWA+GELU, 1 seed for SELU/tanhLN/GELULN — estimated ~3.5h total on RTX A4500
- **Width correlation**: Map block names to channel widths, compute per-block mean final J·s̄, Pearson correlation with width to test the mean-field large-n prediction
- **Exp 5 overhead**: Force J·s̄ via `logit(target)` with near-zero inputs, measure `torch.cuda.max_memory_allocated` and `time.perf_counter` over 20 timing runs, report wall-clock and memory ratios CWA/GELU at each J·s̄ point
- **Fallback**: Always-unrolled mode if IFT gradients produce NaN; batch size reduction for wide OOM; epoch reduction priority order if time is tight
```
